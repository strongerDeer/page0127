# D-1 방문 이력 적재 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로그인한 사용자가 서비스에 들어온 날짜를 하루 1행으로 남겨, 나중에 W1/W4 재방문율을 계산할 수 있게 한다.

**Architecture:** 클라이언트 컴포넌트가 하루 한 번 `POST /api/visit`을 부르고, 라우트가 세션에서 user_id를 직접 읽어 service_role로 `user_daily_visits`에 upsert한다. 테이블은 RLS를 켜고 정책을 두지 않아 전면 거부 상태로 두며, 서버만 쓰고 읽는다. `/api/rum`이 확립한 패턴을 그대로 따른다.

**Tech Stack:** Next.js 16 App Router · Supabase(Postgres + service_role) · vitest(node 환경)

## Global Constraints

- **스펙:** `docs/superpowers/specs/2026-07-28-visit-log-and-rating-split-design.md` (D-1 절)
- **TypeScript strict** — `any` 금지, `unknown` + 타입 가드
- **`type` 우선, `interface` 금지** (ESLint `consistent-type-definitions`)
- **`console.log` 금지** — `console.warn` / `console.error`만 허용
- **컴포넌트는 화살표 + named export** (신규 파일 기준)
- **KST 계산은 `KST_OFFSET_MS = 9 * 60 * 60 * 1000`을 더한 뒤 UTC getter를 쓰는 저장소 관례를 따른다.** `Intl`·`date-fns-tz` 등 새 방식을 도입하지 않는다
- **커밋 메시지는 conventional commits** (`feat(scope):`, `fix(scope):`). **`Co-Authored-By` 트레일러 금지**
- **테스트는 vitest, 파일명 `*.test.ts`** — `e2e/*.spec.ts`(Playwright)와 구분된다. 환경이 `node`라 DOM·브라우저 API를 쓰는 코드는 테스트하지 않는다
- 작업 디렉터리: `apps/page0127` 기준 상대 경로가 대부분이나, **마이그레이션은 저장소 루트의 `supabase/migrations/`** 에 있다

---

### Task 1: KST 날짜 키 유틸

**Files:**
- Modify: `apps/page0127/src/shared/lib/date.ts` (파일 끝에 추가)
- Test: `apps/page0127/src/shared/lib/date.test.ts` (신규)

**Interfaces:**
- Consumes: 없음
- Produces: `toKstDateKey(at: Date | string): string` — `'YYYY-MM-DD'`. Task 3(API 라우트)과 Task 4(VisitReporter)가 **둘 다** 이 함수를 쓴다. 두 곳이 같은 날짜 문자열을 만들어야 중복 전송 억제가 동작한다.

**배경:** 서버는 보통 UTC로 돈다. UTC 날짜로 자르면 한국 시각 밤 9시 이후 방문이 다음 날로 밀려서, 매일 들어온 사용자가 하루 빠진 것처럼 보인다. 한국은 서머타임이 없으므로 고정 +9 오프셋이 항상 정확하다. 같은 방식이 `features/admin-costs/api/getCostSummary.ts`의 `dayKey`와 `shared/lib/aiUsage.ts`에 이미 있다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`apps/page0127/src/shared/lib/date.test.ts` 신규 생성:

```ts
import { describe, expect, it } from 'vitest';

import { toKstDateKey } from './date';

describe('toKstDateKey', () => {
  it('KST 자정 직전은 아직 같은 날이다', () => {
    // 2026-07-28 14:59Z = KST 2026-07-28 23:59
    expect(toKstDateKey('2026-07-28T14:59:00.000Z')).toBe('2026-07-28');
  });

  it('KST 자정에 날짜가 넘어간다', () => {
    // 2026-07-28 15:00Z = KST 2026-07-29 00:00 — UTC로 자르면 28일로 잘못 센다
    expect(toKstDateKey('2026-07-28T15:00:00.000Z')).toBe('2026-07-29');
  });

  it('연말 경계에서도 KST 기준으로 넘어간다', () => {
    expect(toKstDateKey('2026-12-31T15:00:00.000Z')).toBe('2027-01-01');
  });

  it('Date 객체도 받는다', () => {
    expect(toKstDateKey(new Date('2026-07-28T15:00:00.000Z'))).toBe(
      '2026-07-29'
    );
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd apps/page0127 && npx vitest run src/shared/lib/date.test.ts`
Expected: FAIL — `toKstDateKey`가 export되지 않아 import 에러

- [ ] **Step 3: 최소 구현을 넣는다**

`apps/page0127/src/shared/lib/date.ts` **맨 끝에** 추가:

```ts
/**
 * KST(UTC+9) 달력 날짜 키 — 'YYYY-MM-DD'.
 *
 * 서버는 보통 UTC로 돈다. UTC 날짜로 자르면 한국 시각 밤 9시 이후의 일이
 * 다음 날로 밀려서, 매일 들어온 사용자가 하루 빠진 것처럼 보인다.
 * 한국은 서머타임이 없어 고정 오프셋이 항상 정확하다.
 *
 * 같은 계산이 getCostSummary(dayKey)·aiUsage 에도 있다 — 통합 후보지만
 * 그쪽은 이 트랙과 무관해 건드리지 않았다.
 */
const KST_OFFSET_MS = 9 * 60 * 60 * 1000;

export const toKstDateKey = (at: Date | string): string => {
  const d = typeof at === 'string' ? new Date(at) : at;
  // 9시간을 더한 뒤 UTC getter로 읽으면 "KST 벽시계"가 된다
  return new Date(d.getTime() + KST_OFFSET_MS).toISOString().slice(0, 10);
};
```

- [ ] **Step 4: 통과를 확인한다**

Run: `cd apps/page0127 && npx vitest run src/shared/lib/date.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: 커밋**

```bash
git add apps/page0127/src/shared/lib/date.ts apps/page0127/src/shared/lib/date.test.ts
git commit -m "feat(visit): KST 달력 날짜 키 유틸 추가"
```

---

### Task 2: user_daily_visits 테이블

**Files:**
- Create: `supabase/migrations/20260728000002_create_user_daily_visits.sql`

**Interfaces:**
- Consumes: 없음
- Produces: `public.user_daily_visits(user_id uuid, visit_date date, first_visit_at timestamptz)`, PK `(user_id, visit_date)`. Task 3이 이 테이블에 upsert하며 `onConflict: 'user_id,visit_date'`를 쓴다.

**주의 — 파일명 번호:** `20260728000001`은 **이미 두 파일이 쓰고 있다**(`create_quality_rum_samples`, `drop_activity_notifications`). 기존 중복은 운영에 적용된 것을 건드리면 재적용 위험이 있어 그대로 둔다. 새 파일은 `20260728000002`를 쓴다.

- [ ] **Step 1: 마이그레이션 파일을 만든다**

`supabase/migrations/20260728000002_create_user_daily_visits.sql`:

```sql
-- 로그인 사용자의 "그날 왔다" 기록.
--
-- 왜 필요한가:
--   books·activities에는 **행동한 사람**만 남는다. 로그인해서 둘러보고 아무것도 하지
--   않고 나간 사람은 흔적이 없다. auth.users.last_sign_in_at은 최신 1건을 덮어쓰는
--   컬럼이라 이력이 되지 못한다. 재방문율(W1/W4)은 지금부터 적재하지 않으면 소급
--   계산이 불가능하다. 나머지 활성화 지표는 전부 기존 데이터로 소급된다.
--   판단 근거: docs/superpowers/specs/2026-07-28-visit-log-and-rating-split-design.md
--
-- 왜 하루 1행인가:
--   재방문율에 필요한 건 "그날 왔는가"뿐이다. 방문 횟수·페이지·체류시간은 어떤 판단도
--   바꾸지 않는다. 그리고 사용자 id와 묶는 순간 익명 통계가 아니라 **개인별 행동
--   기록**이 되어 개인정보처리방침에 없는 수집이 된다. 페이지 조회수·체류시간은
--   GA·Vercel Analytics가 익명으로 이미 수집 중이다.
--
-- 권한: 쓰기는 /api/visit 라우트가 service_role로(RLS 우회), 읽기는 admin 서버 코드가
--   createAdminClient로 한다. 20260724000000의 `alter default privileges`가 앞으로 생길
--   테이블에 anon/authenticated의 insert 권한까지 주므로, **RLS를 켜고 정책을 하나도
--   만들지 않는 것**이 유일한 차단 수단이다(= 전면 거부). quality_rum_samples와 같은 방식.

create table public.user_daily_visits (
  user_id        uuid not null references auth.users(id) on delete cascade,

  -- KST 달력 날짜. 서버(toKstDateKey)가 계산해 넣는다 — UTC로 자르면 한국 밤 9시
  -- 이후 방문이 다음 날로 밀린다.
  visit_date     date not null,

  -- 그날 처음 들어온 시각. "몇 시에 오는 사용자들인가"를 알려준다(알림·메일 발송
  -- 시각의 근거). upsert가 중복을 무시하므로 자연히 그날 첫 방문 시각이 남는다.
  first_visit_at timestamptz not null default now(),

  -- 복합 PK가 중복 방어의 최종 수단이다. 클라이언트가 몇 번을 보내든 하루 1행.
  primary key (user_id, visit_date)
);

-- 조회는 user_id 기준이라 PK 인덱스로 충분하다. 추가 인덱스를 만들지 않는다.

alter table public.user_daily_visits enable row level security;

comment on table public.user_daily_visits is
  '로그인 사용자의 일별 방문 기록(KST). 재방문율 계산용. 서버(service_role)만 읽고 쓴다.';
```

- [ ] **Step 2: 로컬 DB에 적용해 확인한다**

Run: `supabase db reset`
Expected: 에러 없이 완료. 마지막 줄에 `Finished supabase db reset.`

> 로컬 Docker Supabase가 떠 있어야 한다. 안 떠 있으면 `supabase start` 먼저.

- [ ] **Step 3: 테이블과 잠금 상태를 확인한다**

> **`psql`은 이 머신에 설치돼 있지 않다.** Docker 컨테이너 안의 psql을 쓴다
> (컨테이너 이름은 `docker ps --format '{{.Names}}' | grep db`로 확인 — 이 저장소는
> `supabase_db_0127`).

Run:
```bash
docker exec supabase_db_0127 psql -U postgres -d postgres -c "\d public.user_daily_visits"
```
Expected: 컬럼 3개(`user_id`, `visit_date`, `first_visit_at`), `Primary key: (user_id, visit_date)`

Run:
```bash
docker exec supabase_db_0127 psql -U postgres -d postgres -c \
  "select relrowsecurity from pg_class where relname = 'user_daily_visits';"
docker exec supabase_db_0127 psql -U postgres -d postgres -c \
  "select count(*) from pg_policies where tablename = 'user_daily_visits';"
```
Expected: 첫 번째는 `t`(RLS 켜짐), 두 번째는 `0`(정책 없음 = 전면 거부). **둘 다 맞아야
한다** — RLS만 켜고 정책이 있으면 열려 있는 것이고, 정책이 없어도 RLS가 꺼져 있으면
anon이 그냥 읽는다

- [ ] **Step 4: 커밋**

```bash
git add supabase/migrations/20260728000002_create_user_daily_visits.sql
git commit -m "feat(visit): user_daily_visits 테이블 추가"
```

---

### Task 3: POST /api/visit

**Files:**
- Create: `apps/page0127/app/api/visit/route.ts`

**Interfaces:**
- Consumes: `toKstDateKey` (Task 1), `user_daily_visits` (Task 2), 기존 `getCurrentUser()` from `app/api/_helpers/auth.ts` — 반환 타입 `{ user: User | null, error: NextResponse | null }`
- Produces: `POST /api/visit` — 본문 없음, 항상 204. Task 4의 `VisitReporter`가 호출한다.

**설계 근거 두 가지:**

1. **user_id를 본문으로 받지 않는다.** 브라우저가 "나 길동인데 방문했어요"라고 말하는 걸 믿으면 남의 이름으로 방문 기록을 심을 수 있다. 서버가 세션 쿠키를 보고 직접 판단한다.
2. **항상 204를 돌려준다.** `/api/rum`과 같은 이유 — 수집 엔드포인트가 오류 내용을 돌려주면 공격자에게 검증 규칙만 알려주는 꼴이다. 비로그인도 오류가 아니다(로그아웃 직후 발사된 요청이 정상 경로다).

- [ ] **Step 1: 라우트를 만든다**

`apps/page0127/app/api/visit/route.ts`:

```ts
import { NextResponse } from 'next/server';

import { createAdminClient } from '@/shared/config/supabase/admin';
import { toKstDateKey } from '@/shared/lib/date';

import { getCurrentUser } from '../_helpers/auth';

/**
 * POST /api/visit — 로그인 사용자의 "오늘 왔다" 기록.
 *
 * 왜 Supabase 직접 insert가 아니라 라우트 핸들러인가:
 *   anon 키로 직접 넣게 하려면 테이블에 INSERT 정책을 열어야 하는데, 그러면 누구나
 *   남의 user_id로 방문 기록을 심을 수 있다. 여기서 service_role로 대신 쓰면 테이블은
 *   계속 전면 거부(RLS on + 정책 없음)로 둘 수 있고, **누구인지를 서버가 세션에서
 *   직접 읽는다.**
 *
 * 왜 UA 봇 판정이 없는가(/api/rum과 다른 점):
 *   저장 조건이 "로그인 세션이 있을 것"이라 크롤러는 애초에 통과하지 못한다. 걸러야
 *   할 대상은 로그인해서 도는 e2e 테스트이고, 그건 클라이언트(VisitReporter)가 막는다.
 *
 * 판단 근거: docs/superpowers/specs/2026-07-28-visit-log-and-rating-split-design.md
 */

// 매 방문 발사된다 — 캐시되면 안 되고, 정적 최적화 대상도 아니다.
export const dynamic = 'force-dynamic';

export async function POST() {
  const { user } = await getCurrentUser();

  // 비로그인은 오류가 아니다. 로그아웃 직후 발사된 요청이 정상 경로다.
  if (!user) return new NextResponse(null, { status: 204 });

  // 이미 그날 행이 있으면 아무것도 하지 않는다 → first_visit_at 이 그날 첫 방문
  // 시각으로 유지된다(덮어쓰면 마지막 방문 시각이 되어 뜻이 달라진다).
  const { error } = await createAdminClient()
    .from('user_daily_visits')
    .upsert(
      { user_id: user.id, visit_date: toKstDateKey(new Date()) },
      { onConflict: 'user_id,visit_date', ignoreDuplicates: true }
    );

  if (error) {
    // 수집 실패가 사용자 경험에 영향을 주면 안 된다 — 로그만 남기고 204로 끝낸다.
    console.error('[visit] 방문 기록 저장 실패:', error.message);
  }

  return new NextResponse(null, { status: 204 });
}
```

- [ ] **Step 2: 타입 검사와 린트를 돌린다**

Run: `cd apps/page0127 && npx tsc --noEmit && npx eslint app/api/visit/route.ts`
Expected: 둘 다 출력 없이 통과

- [ ] **Step 3: 비로그인 요청이 204를 받는지 확인한다**

Run:
```bash
cd apps/page0127 && npm run dev &
sleep 8
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:3000/api/visit
```
Expected: `204`

> **Turbopack 함정:** 새로 만든 API 라우트만 404가 나면 코드를 의심하기 전에
> `rm -rf apps/page0127/.next` 후 dev 서버를 다시 띄운다. 라우트 매니페스트가 낡으면
> 새 라우트만 안 잡힌다.

확인 후 dev 서버를 끈다.

- [ ] **Step 4: 커밋**

```bash
git add apps/page0127/app/api/visit/route.ts
git commit -m "feat(visit): POST /api/visit 방문 기록 라우트 추가"
```

---

### Task 4: VisitReporter 컴포넌트와 마운트

**Files:**
- Create: `apps/page0127/src/shared/lib/visit/VisitReporter.tsx`
- Modify: `apps/page0127/app/layout.tsx` (import 추가 + `CurrentUserProvider` 안쪽에 마운트)

**Interfaces:**
- Consumes: `toKstDateKey` (Task 1), `POST /api/visit` (Task 3), 기존 `useCurrentUserContext()` from `@/entities/user` — 반환 `{ currentUser: { id: string; email: string } | null | undefined, isLoading: boolean }`
- Produces: `<VisitReporter />` — 아무것도 렌더하지 않는 effect 전용 컴포넌트

**마운트 위치가 중요하다.** `WebVitalsReporter`는 `CurrentUserProvider` **밖**에 있지만, `VisitReporter`는 로그인 상태를 알아야 하므로 **안쪽**이어야 한다. 밖에 두면 `useCurrentUserContext`가 "Provider 밖에서 사용 불가" 에러를 던진다.

**`sendBeacon`을 쓰지 않는다.** RUM은 페이지가 사라지는 순간 지표가 확정되어 beacon이 필수였지만, 방문 기록은 진입 시점에 이미 확정된다. 그리고 전송 성공을 확인해야 localStorage를 갱신할 수 있는데 `sendBeacon`은 결과를 알려주지 않는다.

- [ ] **Step 1: 컴포넌트를 만든다**

`apps/page0127/src/shared/lib/visit/VisitReporter.tsx`:

```tsx
'use client';

import { useEffect } from 'react';

import { useCurrentUserContext } from '@/entities/user';
import { toKstDateKey } from '@/shared/lib/date';

/**
 * 방문 기록 수집기 — 로그인 사용자의 "오늘 왔다"를 하루 한 번 /api/visit 으로 보낸다.
 *
 * 재방문율(W1/W4)은 지금부터 적재하지 않으면 소급 계산이 불가능하다.
 * 판단 근거: docs/superpowers/specs/2026-07-28-visit-log-and-rating-split-design.md
 *
 * 화면에 아무것도 그리지 않는다(effect 전용). 단 WebVitalsReporter 와 달리
 * **CurrentUserProvider 안쪽**에 마운트해야 한다 — 로그인 상태를 읽기 때문이다.
 */

const ENDPOINT = '/api/visit';
const STORAGE_KEY = 'visit-reported-date';

export const VisitReporter = () => {
  const { currentUser } = useCurrentUserContext();
  const userId = currentUser?.id;

  useEffect(() => {
    // 비로그인(또는 아직 로딩 중)이면 보낼 것이 없다.
    if (!userId) return;

    // 로그인해서 도는 e2e 테스트(Playwright)가 방문 기록을 오염시키지 않게 막는다.
    // 서버는 UA로 거르지 않는다 — 세션이 있어야 저장되므로 크롤러는 못 온다.
    if (navigator.webdriver) return;

    const today = toKstDateKey(new Date());

    let reported: string | null = null;
    try {
      reported = localStorage.getItem(STORAGE_KEY);
    } catch {
      // 프라이빗 모드·차단 설정 — 매번 보내게 되지만 서버 PK가 중복을 흡수한다.
    }
    if (reported === today) return;

    void fetch(ENDPOINT, { method: 'POST' })
      .then((response) => {
        // 실패하면 저장하지 않는다 → 다음 페이지 이동에서 다시 시도한다.
        if (!response.ok) return;
        try {
          localStorage.setItem(STORAGE_KEY, today);
        } catch {
          // 저장 실패는 무시 — 오늘 한 번 더 보낼 뿐이고 서버가 중복을 흡수한다.
        }
      })
      .catch(() => {
        // 수집 실패가 사용자 경험을 건드리면 안 된다 — 조용히 포기한다.
      });
  }, [userId]);

  return null;
};
```

- [ ] **Step 2: layout에 마운트한다**

`apps/page0127/app/layout.tsx` — import 블록에 한 줄 추가 (`@/shared/lib/rum/...` 바로 아래, 알파벳 순서상 `rum` 다음이 `visit`):

```tsx
import { VisitReporter } from '@/shared/lib/visit/VisitReporter';
```

그리고 `<CurrentUserProvider>` **안쪽**, `{children}` 아래에 마운트:

```tsx
          <CurrentUserProvider>
            {children}
            <Toaster />
            {/* 방문 기록 — 로그인 사용자의 "오늘 왔다"를 하루 한 번 남긴다.
                재방문율은 지금부터 쌓지 않으면 소급 계산이 불가능하다.
                CurrentUserProvider 안쪽이어야 한다(로그인 상태를 읽는다). */}
            <VisitReporter />
          </CurrentUserProvider>
```

- [ ] **Step 3: 타입 검사·린트·빌드를 돌린다**

Run:
```bash
cd apps/page0127 && npx tsc --noEmit && npx eslint app/layout.tsx src/shared/lib/visit/VisitReporter.tsx && npm run build
```
Expected: 셋 다 통과. 빌드가 `'server-only'` 위반으로 실패하면 `createAdminClient`가 클라이언트 번들로 샌 것이므로 import 경로를 다시 확인한다

- [ ] **Step 4: 실제로 행이 쌓이는지 눈으로 확인한다**

이 단계는 자동 테스트로 잠글 수 없다(vitest는 node 환경이라 DOM·fetch·localStorage가 없다).

```bash
cd apps/page0127 && npm run dev
```

1. 브라우저로 `http://localhost:3000` 접속해 **로그인**한다
   (로컬은 Google OAuth가 `localhost:3000`에서만 동작한다)
2. 아무 페이지나 연다
3. DB를 확인한다:

```bash
docker exec supabase_db_0127 psql -U postgres -d postgres -c \
  "select user_id, visit_date, first_visit_at from public.user_daily_visits;"
```

Expected: 행 1개. `visit_date`가 **오늘(KST)**이어야 한다

4. 페이지를 여러 번 새로고침한 뒤 같은 쿼리를 다시 돌린다
   Expected: **여전히 행 1개** (하루 1행 억제가 동작). 브라우저 개발자도구 Network 탭에
   `/api/visit` 요청이 첫 1회만 보여야 한다
5. 개발자도구 Application → Local Storage에서 `visit-reported-date`가 오늘 날짜인지 본다

- [ ] **Step 5: 커밋**

```bash
git add apps/page0127/src/shared/lib/visit/VisitReporter.tsx apps/page0127/app/layout.tsx
git commit -m "feat(visit): VisitReporter 마운트로 일별 방문 적재 시작"
```

---

## 완료 조건

- [ ] `cd apps/page0127 && npm run test` 전체 통과
- [ ] `cd apps/page0127 && npx tsc --noEmit` 통과
- [ ] `cd apps/page0127 && npm run build` 통과
- [ ] `supabase db reset` 클린 적용 통과
- [ ] Task 4 Step 4의 수동 확인 완료 (행 1개, 새로고침해도 늘지 않음)

## 이 계획이 만들지 않는 것

- **활성화·재방문 지표 화면** — 실사용자가 0명이라 지금 만들면 전부 0이 뜬다. 활성화 지표는 기존 데이터로 소급 계산되므로 나중에 만들어도 손해가 없다
- **비로그인 방문자 추적** — GA·Vercel Analytics가 익명으로 이미 수집 중이다
- **페이지별 조회수·체류시간** — 같은 이유. 사용자 id와 묶으면 개인별 행동 기록이 된다
