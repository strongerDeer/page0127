# page0127 Admin 페이지 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 관리자(ADMIN_EMAILS 화이트리스트)만 접근하는 `/admin` 페이지에 AI 비용 대시보드와 회원 관리·정지 기능을 만든다.

**Architecture:** 라우트 그룹 `(admin)` + 공통 게이트. 접근통제는 미들웨어(로그인 필수) + 서버 헬퍼 `assertAdmin()`(관리자 판정, 진짜 경계) 2중. 여러 유저 데이터를 가로지르는 조회/변경은 전부 `createAdminClient()`(service_role, RLS 우회). 정지는 Supabase Auth 네이티브 ban(실제 차단) + `profiles.status`/`suspended_until` 미러(표시용) + `admin_actions` 감사 로그를 한 서버액션이 함께 쓴다.

**Tech Stack:** Next.js 16 App Router(Server Component 우선), FSD, Supabase(Postgres + Auth), Tailwind v4 + shadcn/ui, vitest(순수 로직 단위 테스트).

## Global Constraints

- 패키지 매니저: **npm** (workspaces). 설치는 `--workspace=apps/page0127`.
- 접근통제: `ADMIN_EMAILS`(서버 전용 env, 쉼표 구분). 비관리자는 `notFound()`(404).
- admin 데이터 접근은 **반드시** `createAdminClient()`. 일반 `createClient()`는 RLS "본인만"에 막힌다.
- `cost_in_cents`는 **USD 센트**. 예산은 `MONTHLY_BUDGET_KRW=30000`, 환산 `USD_TO_KRW=1400`(상수).
- 사용자별 순위는 **비용이 아니라 호출 건수**(`ai_usage_logs`). 궁합 분석은 두 사용자 쌍이라 비용 단일 귀속 불가.
- 디자인: 그림자 없음(입체는 `1px solid var(--line)`), 헤딩·라벨에 이모지 없음(lucide 단색 아이콘), 종이·잉크 팔레트, 데이터 없으면 빈 상태 문구. `'use client'`는 상호작용 필요한 곳만.
- 검증(하이브리드): 순수 로직은 vitest, 그 외는 `npm run type-check`·`npm run build`·개발서버 수동 확인.
- 커밋: 작업 브랜치 `feat/page0127-admin`. 각 태스크 끝에 커밋.

---

## 파일 구조 (생성/수정)

**Phase 0 — 기반**
- Create `apps/page0127/vitest.config.ts` — vitest 설정
- Modify `apps/page0127/package.json` — vitest devDep + `test` 스크립트
- Create `apps/page0127/src/shared/lib/admin/config.ts` — 화이트리스트 파싱/판정 + 상수
- Create `apps/page0127/src/shared/lib/admin/config.test.ts`
- Create `apps/page0127/src/shared/lib/admin/assertAdmin.ts` — 서버 가드
- Modify `apps/page0127/src/shared/config/supabase/middleware.ts` — `/admin` 보호
- Modify `apps/page0127/.env.example` — `ADMIN_EMAILS` 문서화
- Create `apps/page0127/src/widgets/admin/ui/AdminNav.tsx` — 사이드바 nav
- Create `apps/page0127/app/(admin)/admin/layout.tsx` — 게이트 + 셸
- Create `apps/page0127/app/(admin)/admin/page.tsx` — 대시보드 홈

**Phase 1 — 마이그레이션**
- Create `supabase/migrations/20260723000002_admin_status_and_actions.sql`

**Phase 2 — AI 비용 대시보드**
- Create `apps/page0127/src/features/admin-costs/lib/cost.ts` — 순수 계산
- Create `apps/page0127/src/features/admin-costs/lib/cost.test.ts`
- Create `apps/page0127/src/features/admin-costs/api/getCostSummary.ts` — 서버 쿼리
- Create `apps/page0127/src/features/admin-costs/ui/CostDashboard.tsx` — 표시 UI
- Create `apps/page0127/app/(admin)/admin/costs/page.tsx`

**Phase 3 — 회원 관리·정지**
- Create `apps/page0127/src/features/admin-members/lib/suspension.ts` — 순수 계산
- Create `apps/page0127/src/features/admin-members/lib/suspension.test.ts`
- Create `apps/page0127/src/features/admin-members/api/getMembers.ts` — 목록 쿼리
- Create `apps/page0127/src/features/admin-members/api/getMemberDetail.ts` — 상세 쿼리
- Create `apps/page0127/src/features/admin-members/api/suspendActions.ts` — 서버액션
- Create `apps/page0127/src/features/admin-members/ui/MemberTable.tsx`
- Create `apps/page0127/src/features/admin-members/ui/SuspendForm.tsx` — `'use client'`
- Create `apps/page0127/app/(admin)/admin/members/page.tsx`
- Create `apps/page0127/app/(admin)/admin/members/[id]/page.tsx`

---

## Task 1: vitest 설치 & 설정

**Files:**
- Modify: `apps/page0127/package.json`
- Create: `apps/page0127/vitest.config.ts`

**Interfaces:**
- Produces: `npm run test`(= `vitest run`), `npm run test:watch`. 순수 로직 테스트를 `src/**/*.test.ts`에서 수집.

- [ ] **Step 1: vitest 설치** (사용자가 직접 입력)

```bash
npm install -D vitest --workspace=apps/page0127
```

- [ ] **Step 2: vitest 설정 파일 생성**

`apps/page0127/vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config';

// 순수 함수 단위 테스트만 돌린다(노드 환경).
// 화면·서버 연결은 type-check/build/개발서버로 확인한다.
export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
});
```

- [ ] **Step 3: package.json 스크립트 추가**

`apps/page0127/package.json`의 `scripts`에 추가:
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 4: 동작 확인**

Run: `npm run test --workspace=apps/page0127`
Expected: "No test files found" (아직 테스트 없음) — 에러 없이 종료.

- [ ] **Step 5: 커밋**

```bash
git add apps/page0127/package.json apps/page0127/vitest.config.ts apps/page0127/package-lock.json package-lock.json
git commit -m "chore(admin): vitest 도입 — 순수 로직 단위 테스트용"
```

---

## Task 2: admin config — 화이트리스트 파싱/판정 + 상수 (TDD)

**Files:**
- Create: `apps/page0127/src/shared/lib/admin/config.ts`
- Test: `apps/page0127/src/shared/lib/admin/config.test.ts`

**Interfaces:**
- Produces:
  - `parseAdminEmails(raw: string | undefined): string[]`
  - `isAdminEmail(email: string | null | undefined, adminEmails: string[]): boolean`
  - `getAdminEmails(): string[]` — `process.env.ADMIN_EMAILS` 기준
  - `USD_TO_KRW: number`, `MONTHLY_BUDGET_KRW: number`

- [ ] **Step 1: 실패하는 테스트 작성**

`config.test.ts`:
```ts
import { describe, it, expect } from 'vitest';

import { parseAdminEmails, isAdminEmail } from './config';

describe('parseAdminEmails', () => {
  it('빈 값이면 빈 배열', () => {
    expect(parseAdminEmails(undefined)).toEqual([]);
    expect(parseAdminEmails('')).toEqual([]);
  });
  it('쉼표로 나누고 소문자·trim 정규화', () => {
    expect(parseAdminEmails(' A@x.com , b@Y.com ')).toEqual([
      'a@x.com',
      'b@y.com',
    ]);
  });
  it('빈 항목은 버린다', () => {
    expect(parseAdminEmails('a@x.com,,')).toEqual(['a@x.com']);
  });
});

describe('isAdminEmail', () => {
  const admins = ['a@x.com'];
  it('대소문자·공백 무시하고 매칭', () => {
    expect(isAdminEmail(' A@X.com ', admins)).toBe(true);
  });
  it('없는 이메일은 false', () => {
    expect(isAdminEmail('z@x.com', admins)).toBe(false);
  });
  it('null/undefined는 false', () => {
    expect(isAdminEmail(null, admins)).toBe(false);
    expect(isAdminEmail(undefined, admins)).toBe(false);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npm run test --workspace=apps/page0127`
Expected: FAIL — `./config` 모듈 없음.

- [ ] **Step 3: 최소 구현**

`config.ts`:
```ts
/**
 * 관리자 이메일 화이트리스트 파싱·판정 + 비용 대시보드 상수
 *
 * 학습 포인트: 화이트리스트 비교 로직을 edge(미들웨어)가 아니라
 * 이 순수 함수 한 곳에 모아 테스트 가능하게 둔다.
 */

/** ADMIN_EMAILS 원문(쉼표 구분)을 정규화된 이메일 배열로 파싱 */
export function parseAdminEmails(raw: string | undefined): string[] {
  if (!raw) return [];
  return raw
    .split(',')
    .map((e) => e.trim().toLowerCase())
    .filter((e) => e.length > 0);
}

/** 이메일이 화이트리스트에 있는지 (대소문자·공백 무시) */
export function isAdminEmail(
  email: string | null | undefined,
  adminEmails: string[]
): boolean {
  if (!email) return false;
  return adminEmails.includes(email.trim().toLowerCase());
}

/** 현재 환경변수 기준 관리자 목록 */
export function getAdminEmails(): string[] {
  return parseAdminEmails(process.env.ADMIN_EMAILS);
}

/** USD → KRW 환산 근사 상수 (수동 갱신) */
export const USD_TO_KRW = 1400;

/** 월 예산 (원) */
export const MONTHLY_BUDGET_KRW = 30000;
```

- [ ] **Step 4: 통과 확인**

Run: `npm run test --workspace=apps/page0127`
Expected: PASS (6 테스트).

- [ ] **Step 5: 커밋**

```bash
git add apps/page0127/src/shared/lib/admin/config.ts apps/page0127/src/shared/lib/admin/config.test.ts
git commit -m "feat(admin): 관리자 화이트리스트 판정 + 비용 상수"
```

---

## Task 3: assertAdmin 서버 가드

**Files:**
- Create: `apps/page0127/src/shared/lib/admin/assertAdmin.ts`

**Interfaces:**
- Consumes: `getAdminEmails`, `isAdminEmail`(Task 2); `createClient`(`@/shared/config/supabase/server`); `notFound`(`next/navigation`).
- Produces: `assertAdmin(): Promise<User>` — 관리자가 아니면 `notFound()`(404), 맞으면 `User` 반환.

- [ ] **Step 1: 구현 작성**

`assertAdmin.ts`:
```ts
import { notFound } from 'next/navigation';

import type { User } from '@supabase/supabase-js';

import { createClient } from '@/shared/config/supabase/server';
import { getAdminEmails, isAdminEmail } from '@/shared/lib/admin/config';

/**
 * 현재 요청 유저가 관리자인지 확인한다.
 *
 * 진짜 보안 경계 — admin 레이아웃과 모든 admin 서버액션 첫 줄에서 호출한다.
 * 관리자가 아니면(비로그인 포함) notFound()로 404 → admin 존재 자체를 숨긴다.
 *
 * 여기서는 신원 확인만 하므로 일반 createClient(쿠키 세션)로 충분하다.
 * 실제 데이터 조회는 호출부에서 createAdminClient(RLS 우회)를 쓴다.
 */
export async function assertAdmin(): Promise<User> {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  // notFound()는 never를 반환하므로 이후 user는 non-null로 좁혀진다
  if (!user || !isAdminEmail(user.email, getAdminEmails())) {
    notFound();
  }
  return user;
}
```

- [ ] **Step 2: 타입 확인**

Run: `npm run type-check --workspace=apps/page0127`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add apps/page0127/src/shared/lib/admin/assertAdmin.ts
git commit -m "feat(admin): assertAdmin 서버 가드 — 비관리자 404"
```

---

## Task 4: 미들웨어 /admin 보호 + .env.example

**Files:**
- Modify: `apps/page0127/src/shared/config/supabase/middleware.ts`
- Modify: `apps/page0127/.env.example`

**Interfaces:**
- Produces: `/admin`·하위 경로에 비로그인 접근 시 `/login` 리다이렉트.

- [ ] **Step 1: PROTECTED_PREFIXES에 '/admin' 추가**

`middleware.ts`의 `PROTECTED_PREFIXES` 배열을 수정:
```ts
  const PROTECTED_PREFIXES = [
    '/admin',
    '/books',
    '/feed',
    '/search',
    '/settings',
    '/notifications',
  ];
```

- [ ] **Step 2: .env.example에 ADMIN_EMAILS 문서화**

`.env.example`에 추가(설명 주석과 함께):
```
# 관리자 이메일 화이트리스트 (쉼표 구분). /admin 접근 허용 대상.
ADMIN_EMAILS=you@example.com
```

- [ ] **Step 3: 타입·빌드 확인**

Run: `npm run type-check --workspace=apps/page0127`
Expected: 에러 없음.

- [ ] **Step 4: 커밋**

```bash
git add apps/page0127/src/shared/config/supabase/middleware.ts apps/page0127/.env.example
git commit -m "feat(admin): /admin 미들웨어 보호 + ADMIN_EMAILS 문서화"
```

---

## Task 5: admin 셸 — 레이아웃 + nav + 대시보드 홈

**Files:**
- Create: `apps/page0127/src/widgets/admin/ui/AdminNav.tsx`
- Create: `apps/page0127/app/(admin)/admin/layout.tsx`
- Create: `apps/page0127/app/(admin)/admin/page.tsx`

**Interfaces:**
- Consumes: `assertAdmin`(Task 3).
- Produces: `/admin` 진입 시 관리자만 셸(사이드바 nav + 콘텐츠) 렌더. 비관리자 404.

- [ ] **Step 1: AdminNav 작성** (Server Component — 링크만이라 client 불필요)

`AdminNav.tsx`:
```tsx
import Link from 'next/link';

import { LayoutDashboard, Receipt, Users } from 'lucide-react';

// 이모지 대신 lucide 단색 아이콘, 입체는 1px 선으로만 표현한다.
const NAV = [
  { href: '/admin', label: '대시보드', icon: LayoutDashboard },
  { href: '/admin/costs', label: 'AI 비용', icon: Receipt },
  { href: '/admin/members', label: '회원 관리', icon: Users },
];

export function AdminNav() {
  return (
    <nav className="flex flex-col gap-1 p-4">
      {NAV.map(({ href, label, icon: Icon }) => (
        <Link
          key={href}
          href={href}
          className="flex items-center gap-2 rounded px-3 py-2 text-sm hover:bg-[var(--surface-2)]"
        >
          <Icon className="h-4 w-4" aria-hidden />
          {label}
        </Link>
      ))}
    </nav>
  );
}
```

- [ ] **Step 2: admin 레이아웃 작성** (게이트 + 셸)

`app/(admin)/admin/layout.tsx`:
```tsx
import type { ReactNode } from 'react';

import { AdminNav } from '@/widgets/admin/ui/AdminNav';
import { assertAdmin } from '@/shared/lib/admin/assertAdmin';

// 페이지 접근 게이트. 데이터 접근 게이트는 각 서버액션/쿼리에서 재확인한다.
export default async function AdminLayout({
  children,
}: {
  children: ReactNode;
}) {
  await assertAdmin();

  return (
    <div className="mx-auto flex min-h-screen max-w-6xl">
      <aside className="w-56 border-r border-[var(--line)]">
        <div className="border-b border-[var(--line)] px-4 py-4 text-sm font-semibold">
          운영 콘솔
        </div>
        <AdminNav />
      </aside>
      <main className="flex-1 p-6">{children}</main>
    </div>
  );
}
```

- [ ] **Step 3: 대시보드 홈 작성**

`app/(admin)/admin/page.tsx`:
```tsx
import Link from 'next/link';

const CARDS = [
  { href: '/admin/costs', title: 'AI 비용', desc: '이번 달 사용액과 예산 대비' },
  { href: '/admin/members', title: '회원 관리', desc: '가입자 조회·정지' },
];

export default function AdminHomePage() {
  return (
    <section className="grid gap-4 sm:grid-cols-2">
      {CARDS.map((c) => (
        <Link
          key={c.href}
          href={c.href}
          className="rounded border border-[var(--line)] p-4 hover:bg-[var(--surface-2)]"
        >
          <div className="text-sm font-semibold">{c.title}</div>
          <p className="mt-1 text-sm text-[var(--ink-soft)]">{c.desc}</p>
        </Link>
      ))}
    </section>
  );
}
```

> 색 토큰(`--line`, `--surface-2`, `--ink-soft`)은 프로젝트 토큰명에 맞춰 조정. 없으면 기존 랜딩/서재에서 쓰는 토큰으로 대체.

- [ ] **Step 4: 빌드·수동 확인**

Run: `npm run build --workspace=apps/page0127`
그다음 개발서버로 확인: `npm run dev --workspace=apps/page0127`
- 관리자 이메일로 로그인 후 `/admin` → 셸이 보인다.
- 로그아웃 상태에서 `/admin` → `/login` 리다이렉트.
- 비관리자 로그인 상태에서 `/admin` → 404.

- [ ] **Step 5: 커밋**

```bash
git add apps/page0127/src/widgets/admin apps/page0127/app/\(admin\)
git commit -m "feat(admin): 셸 레이아웃·네비게이션·대시보드 홈"
```

---

## Task 6: DB 마이그레이션 — profiles 상태 + admin_actions

**Files:**
- Create: `supabase/migrations/20260723000002_admin_status_and_actions.sql`

**Interfaces:**
- Produces: `profiles.status`('active'|'suspended'), `profiles.suspended_until`; `admin_actions` 테이블.

- [ ] **Step 1: 마이그레이션 파일 작성**

```sql
-- Admin: 회원 정지 상태 미러 + 관리자 행위 감사 로그
-- 작성일: 2026-07-23

-- 1) profiles: 정지 상태(표시용 미러). 실제 차단은 Supabase Auth 네이티브 ban.
ALTER TABLE profiles
  ADD COLUMN IF NOT EXISTS status text NOT NULL DEFAULT 'active'
    CHECK (status IN ('active', 'suspended')),
  ADD COLUMN IF NOT EXISTS suspended_until timestamptz;
-- suspended_until: null = 영구(정지 중) 또는 미정지. 값 있으면 그 시각까지 임시 정지.

-- 2) admin_actions: 누가·언제·왜 정지/해제했는지
CREATE TABLE IF NOT EXISTS admin_actions (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  admin_email text NOT NULL,
  target_user_id uuid REFERENCES auth.users(id) ON DELETE SET NULL,
  action text NOT NULL,          -- 'suspend' | 'unsuspend'
  reason text,
  duration_days int,             -- null = 영구
  created_at timestamptz NOT NULL DEFAULT now()
);

-- RLS: admin은 service_role로 우회하므로 정책 없이 활성화만(일반 유저 차단)
ALTER TABLE admin_actions ENABLE ROW LEVEL SECURITY;
```

- [ ] **Step 2: 적용** (사용자가 직접 입력 — Supabase 프로젝트에 적용)

```bash
# 로컬 supabase 사용 시
supabase db push
# 또는 Supabase 대시보드 SQL Editor에 위 SQL 실행
```

- [ ] **Step 3: 커밋**

```bash
git add supabase/migrations/20260723000002_admin_status_and_actions.sql
git commit -m "feat(admin): profiles 정지 상태 + admin_actions 감사 로그 마이그레이션"
```

---

## Task 7: 비용 계산 순수 로직 (TDD)

**Files:**
- Create: `apps/page0127/src/features/admin-costs/lib/cost.ts`
- Test: `apps/page0127/src/features/admin-costs/lib/cost.test.ts`

**Interfaces:**
- Produces:
  - `type CostRow = { cost_in_cents: number | null }`
  - `sumCents(rows: CostRow[]): number`
  - `type BudgetUsage = { usd: number; krw: number; budgetKrw: number; percent: number }`
  - `computeBudgetUsage(totalCents: number, opts: { usdToKrw: number; budgetKrw: number }): BudgetUsage`
  - `formatUsd(usd: number): string`, `formatKrw(krw: number): string`

- [ ] **Step 1: 실패하는 테스트 작성**

`cost.test.ts`:
```ts
import { describe, it, expect } from 'vitest';

import { sumCents, computeBudgetUsage, formatUsd, formatKrw } from './cost';

describe('sumCents', () => {
  it('null은 0으로 취급해 합산', () => {
    expect(sumCents([{ cost_in_cents: 120 }, { cost_in_cents: null }, { cost_in_cents: 80 }])).toBe(200);
  });
  it('빈 배열은 0', () => {
    expect(sumCents([])).toBe(0);
  });
});

describe('computeBudgetUsage', () => {
  it('센트→달러→원 환산과 퍼센트', () => {
    // 820센트 = $8.20, ×1400 = 11480원, / 30000 = 38%
    const u = computeBudgetUsage(820, { usdToKrw: 1400, budgetKrw: 30000 });
    expect(u.usd).toBeCloseTo(8.2);
    expect(u.krw).toBe(11480);
    expect(u.percent).toBe(38);
  });
  it('예산이 0이면 percent 0', () => {
    expect(computeBudgetUsage(100, { usdToKrw: 1400, budgetKrw: 0 }).percent).toBe(0);
  });
});

describe('format', () => {
  it('달러는 소수 2자리', () => {
    expect(formatUsd(8.2)).toBe('$8.20');
  });
  it('원은 천단위 콤마', () => {
    expect(formatKrw(11480)).toBe('₩11,480');
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npm run test --workspace=apps/page0127`
Expected: FAIL — `./cost` 없음.

- [ ] **Step 3: 최소 구현**

`cost.ts`:
```ts
export type CostRow = { cost_in_cents: number | null };

/** cost_in_cents 합계 (null 무시). 단위: USD 센트 */
export function sumCents(rows: CostRow[]): number {
  return rows.reduce((acc, r) => acc + (r.cost_in_cents ?? 0), 0);
}

export type BudgetUsage = {
  usd: number; // 달러
  krw: number; // 원(반올림 정수)
  budgetKrw: number;
  percent: number; // 0~100+ (반올림 정수)
};

export function computeBudgetUsage(
  totalCents: number,
  opts: { usdToKrw: number; budgetKrw: number }
): BudgetUsage {
  const usd = totalCents / 100;
  const krw = Math.round(usd * opts.usdToKrw);
  const percent =
    opts.budgetKrw > 0 ? Math.round((krw / opts.budgetKrw) * 100) : 0;
  return { usd, krw, budgetKrw: opts.budgetKrw, percent };
}

export function formatUsd(usd: number): string {
  return `$${usd.toFixed(2)}`;
}

export function formatKrw(krw: number): string {
  return `₩${krw.toLocaleString('ko-KR')}`;
}
```

- [ ] **Step 4: 통과 확인**

Run: `npm run test --workspace=apps/page0127`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add apps/page0127/src/features/admin-costs/lib
git commit -m "feat(admin): 비용 집계·예산 환산 순수 로직"
```

---

## Task 8: getCostSummary 서버 쿼리

**Files:**
- Create: `apps/page0127/src/features/admin-costs/api/getCostSummary.ts`

**Interfaces:**
- Consumes: `createAdminClient`(`@/shared/config/supabase/admin`), `sumCents`(Task 7).
- Produces:
  - `type CostSummary = { monthTotalCents: number; daily: { date: string; cents: number }[]; byFeature: { taste: { cents: number; count: number }; compatibility: { cents: number; count: number } }; topUsers: { userId: string; count: number }[] }`
  - `getCostSummary(): Promise<CostSummary>`

- [ ] **Step 1: 구현 작성**

`getCostSummary.ts`:
```ts
import { createAdminClient } from '@/shared/config/supabase/admin';

import { sumCents } from '../lib/cost';

export type CostSummary = {
  monthTotalCents: number;
  daily: { date: string; cents: number }[];
  byFeature: {
    taste: { cents: number; count: number };
    compatibility: { cents: number; count: number };
  };
  topUsers: { userId: string; count: number }[];
};

// 이번 달 1일 00:00(로컬 근사, UTC)로 시작 경계를 잡는다.
function monthStartISO(now = new Date()): string {
  return new Date(Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), 1)).toISOString();
}

function dayKey(iso: string): string {
  return iso.slice(0, 10); // YYYY-MM-DD
}

export async function getCostSummary(): Promise<CostSummary> {
  const supabase = createAdminClient();
  const since = monthStartISO();

  const [taste, compat, usage] = await Promise.all([
    supabase
      .from('taste_analyses')
      .select('cost_in_cents, created_at')
      .gte('created_at', since),
    supabase
      .from('compatibility_analyses')
      .select('cost_in_cents, created_at')
      .gte('created_at', since),
    supabase
      .from('ai_usage_logs')
      .select('user_id, feature, created_at')
      .gte('created_at', since),
  ]);

  const tasteRows = taste.data ?? [];
  const compatRows = compat.data ?? [];
  const usageRows = usage.data ?? [];

  const monthTotalCents = sumCents(tasteRows) + sumCents(compatRows);

  // 일별 비용 합산
  const dailyMap = new Map<string, number>();
  for (const r of [...tasteRows, ...compatRows]) {
    const k = dayKey(r.created_at);
    dailyMap.set(k, (dailyMap.get(k) ?? 0) + (r.cost_in_cents ?? 0));
  }
  const daily = [...dailyMap.entries()]
    .map(([date, cents]) => ({ date, cents }))
    .sort((a, b) => a.date.localeCompare(b.date));

  // 기능별 건수 (ai_usage_logs), 비용은 각 분석 테이블 합
  const count = (f: string) => usageRows.filter((u) => u.feature === f).length;
  const byFeature = {
    taste: { cents: sumCents(tasteRows), count: count('taste_analysis') },
    compatibility: { cents: sumCents(compatRows), count: count('compatibility') },
  };

  // 사용자별 호출 건수 Top 10 (비용 아님 — 궁합은 단일 귀속 불가)
  const userMap = new Map<string, number>();
  for (const u of usageRows) {
    userMap.set(u.user_id, (userMap.get(u.user_id) ?? 0) + 1);
  }
  const topUsers = [...userMap.entries()]
    .map(([userId, c]) => ({ userId, count: c }))
    .sort((a, b) => b.count - a.count)
    .slice(0, 10);

  return { monthTotalCents, daily, byFeature, topUsers };
}
```

- [ ] **Step 2: 타입 확인**

Run: `npm run type-check --workspace=apps/page0127`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add apps/page0127/src/features/admin-costs/api
git commit -m "feat(admin): 이번 달 비용 요약 서버 쿼리"
```

---

## Task 9: 비용 대시보드 UI + 페이지

**Files:**
- Create: `apps/page0127/src/features/admin-costs/ui/CostDashboard.tsx`
- Create: `apps/page0127/app/(admin)/admin/costs/page.tsx`

**Interfaces:**
- Consumes: `CostSummary`(Task 8), `computeBudgetUsage`·`formatUsd`·`formatKrw`(Task 7), `USD_TO_KRW`·`MONTHLY_BUDGET_KRW`(Task 2), `getCostSummary`(Task 8).

- [ ] **Step 1: CostDashboard 표시 컴포넌트 작성** (Server Component — 상호작용 없음)

`CostDashboard.tsx`:
```tsx
import {
  computeBudgetUsage,
  formatKrw,
  formatUsd,
} from '@/features/admin-costs/lib/cost';
import type { CostSummary } from '@/features/admin-costs/api/getCostSummary';
import { MONTHLY_BUDGET_KRW, USD_TO_KRW } from '@/shared/lib/admin/config';

export function CostDashboard({ summary }: { summary: CostSummary }) {
  const usage = computeBudgetUsage(summary.monthTotalCents, {
    usdToKrw: USD_TO_KRW,
    budgetKrw: MONTHLY_BUDGET_KRW,
  });

  return (
    <div className="flex flex-col gap-6">
      {/* 예산 게이지 */}
      <section className="rounded border border-[var(--line)] p-4">
        <div className="text-sm text-[var(--ink-soft)]">이번 달 AI 비용</div>
        <div className="mt-1 text-lg font-semibold">
          {formatUsd(usage.usd)} ≈ {formatKrw(usage.krw)} / {formatKrw(usage.budgetKrw)}{' '}
          ({usage.percent}%)
        </div>
        <div className="mt-2 h-2 w-full border border-[var(--line)]">
          <div
            className="h-full bg-[var(--ink)]"
            style={{ width: `${Math.min(usage.percent, 100)}%` }}
          />
        </div>
      </section>

      {/* 기능별 분해 */}
      <section className="grid gap-4 sm:grid-cols-2">
        {(['taste', 'compatibility'] as const).map((f) => (
          <div key={f} className="rounded border border-[var(--line)] p-4">
            <div className="text-sm font-semibold">
              {f === 'taste' ? '취향 분석' : '궁합 분석'}
            </div>
            <div className="mt-1 text-sm text-[var(--ink-soft)]">
              {summary.byFeature[f].count}회 ·{' '}
              {formatUsd(summary.byFeature[f].cents / 100)}
            </div>
          </div>
        ))}
      </section>

      {/* 일별 추이 */}
      <section className="rounded border border-[var(--line)] p-4">
        <div className="text-sm font-semibold">일별 추이 (이번 달)</div>
        {summary.daily.length === 0 ? (
          <p className="mt-2 text-sm text-[var(--ink-soft)]">
            아직 이번 달 사용 내역이 없습니다.
          </p>
        ) : (
          <ul className="mt-2 flex flex-col gap-1 text-sm">
            {summary.daily.map((d) => (
              <li key={d.date} className="flex justify-between">
                <span className="text-[var(--ink-soft)]">{d.date}</span>
                <span>{formatUsd(d.cents / 100)}</span>
              </li>
            ))}
          </ul>
        )}
      </section>

      {/* 사용자별 호출 순위 */}
      <section className="rounded border border-[var(--line)] p-4">
        <div className="text-sm font-semibold">많이 쓴 사용자 (호출 횟수)</div>
        {summary.topUsers.length === 0 ? (
          <p className="mt-2 text-sm text-[var(--ink-soft)]">데이터 없음</p>
        ) : (
          <ol className="mt-2 flex flex-col gap-1 text-sm">
            {summary.topUsers.map((u, i) => (
              <li key={u.userId} className="flex justify-between">
                <span className="text-[var(--ink-soft)]">
                  {i + 1}. {u.userId.slice(0, 8)}…
                </span>
                <span>{u.count}회</span>
              </li>
            ))}
          </ol>
        )}
      </section>
    </div>
  );
}
```

- [ ] **Step 2: costs 페이지 작성**

`app/(admin)/admin/costs/page.tsx`:
```tsx
import { getCostSummary } from '@/features/admin-costs/api/getCostSummary';
import { CostDashboard } from '@/features/admin-costs/ui/CostDashboard';

export default async function AdminCostsPage() {
  const summary = await getCostSummary();
  return (
    <section>
      <h1 className="mb-4 text-base font-semibold">AI 비용</h1>
      <CostDashboard summary={summary} />
    </section>
  );
}
```

- [ ] **Step 3: 빌드·수동 확인**

Run: `npm run build --workspace=apps/page0127`
개발서버에서 관리자로 `/admin/costs` → 게이지·기능별·일별·순위가 렌더(데이터 없으면 빈 상태 문구).

- [ ] **Step 4: 커밋**

```bash
git add apps/page0127/src/features/admin-costs/ui apps/page0127/app/\(admin\)/admin/costs
git commit -m "feat(admin): AI 비용 대시보드 화면"
```

---

## Task 10: 정지 계산 순수 로직 (TDD)

**Files:**
- Create: `apps/page0127/src/features/admin-members/lib/suspension.ts`
- Test: `apps/page0127/src/features/admin-members/lib/suspension.test.ts`

**Interfaces:**
- Produces:
  - `type SuspendInput = { kind: 'permanent' } | { kind: 'days'; days: number }`
  - `type BanParams = { banDuration: string; suspendedUntil: string | null }`
  - `computeBan(input: SuspendInput, now: Date): BanParams`
  - `isCurrentlySuspended(status: string, suspendedUntil: string | null, now: Date): boolean`

- [ ] **Step 1: 실패하는 테스트 작성**

`suspension.test.ts`:
```ts
import { describe, it, expect } from 'vitest';

import { computeBan, isCurrentlySuspended } from './suspension';

describe('computeBan', () => {
  it('영구 정지: 긴 기간 + suspendedUntil null', () => {
    const r = computeBan({ kind: 'permanent' }, new Date('2026-07-23T00:00:00Z'));
    expect(r.banDuration).toBe('876000h');
    expect(r.suspendedUntil).toBeNull();
  });
  it('7일 정지: 168h + 7일 뒤 시각', () => {
    const now = new Date('2026-07-23T00:00:00Z');
    const r = computeBan({ kind: 'days', days: 7 }, now);
    expect(r.banDuration).toBe('168h');
    expect(r.suspendedUntil).toBe('2026-07-30T00:00:00.000Z');
  });
});

describe('isCurrentlySuspended', () => {
  const now = new Date('2026-07-23T00:00:00Z');
  it('active면 false', () => {
    expect(isCurrentlySuspended('active', null, now)).toBe(false);
  });
  it('suspended + until null(영구)이면 true', () => {
    expect(isCurrentlySuspended('suspended', null, now)).toBe(true);
  });
  it('suspended + 미래 until이면 true', () => {
    expect(isCurrentlySuspended('suspended', '2026-08-01T00:00:00Z', now)).toBe(true);
  });
  it('suspended + 과거 until이면 false(만료 자가치유)', () => {
    expect(isCurrentlySuspended('suspended', '2026-07-01T00:00:00Z', now)).toBe(false);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npm run test --workspace=apps/page0127`
Expected: FAIL — `./suspension` 없음.

- [ ] **Step 3: 최소 구현**

`suspension.ts`:
```ts
/** 정지 기간 입력 */
export type SuspendInput =
  | { kind: 'permanent' }
  | { kind: 'days'; days: number };

/** Supabase ban_duration + profiles.suspended_until 미러 값 */
export type BanParams = {
  banDuration: string;
  suspendedUntil: string | null; // null = 영구
};

const PERMANENT_BAN = '876000h'; // ≈ 100년

export function computeBan(input: SuspendInput, now: Date): BanParams {
  if (input.kind === 'permanent') {
    return { banDuration: PERMANENT_BAN, suspendedUntil: null };
  }
  const hours = input.days * 24;
  const until = new Date(now.getTime() + input.days * 24 * 60 * 60 * 1000);
  return { banDuration: `${hours}h`, suspendedUntil: until.toISOString() };
}

/**
 * 목록/상세 표시용: 지금 정지 중인가.
 * 임시 정지는 Supabase 쪽에서 만료돼도 미러가 남으므로,
 * 읽는 시점에 계산해 자가 치유한다.
 */
export function isCurrentlySuspended(
  status: string,
  suspendedUntil: string | null,
  now: Date
): boolean {
  if (status !== 'suspended') return false;
  if (suspendedUntil === null) return true; // 영구
  return new Date(suspendedUntil).getTime() > now.getTime();
}
```

- [ ] **Step 4: 통과 확인**

Run: `npm run test --workspace=apps/page0127`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add apps/page0127/src/features/admin-members/lib
git commit -m "feat(admin): 정지 기간·현재 정지여부 순수 로직"
```

---

## Task 11: 회원 목록·상세 쿼리

**Files:**
- Create: `apps/page0127/src/features/admin-members/api/getMembers.ts`
- Create: `apps/page0127/src/features/admin-members/api/getMemberDetail.ts`

**Interfaces:**
- Consumes: `createAdminClient`, `isCurrentlySuspended`(Task 10).
- Produces:
  - `type MemberRow = { id: string; email: string | null; nickname: string | null; username: string | null; createdAt: string; bookCount: number; suspended: boolean }`
  - `getMembers(opts: { search?: string; page?: number; pageSize?: number }): Promise<{ rows: MemberRow[]; total: number }>`
  - `type MemberDetail = { id: string; email: string | null; nickname: string | null; username: string | null; createdAt: string; bookCount: number; aiUsageCount: number; suspended: boolean; suspendedUntil: string | null }`
  - `getMemberDetail(id: string): Promise<MemberDetail | null>`

- [ ] **Step 1: getMembers 작성**

`getMembers.ts`:
```ts
import { createAdminClient } from '@/shared/config/supabase/admin';

import { isCurrentlySuspended } from '../lib/suspension';

export type MemberRow = {
  id: string;
  email: string | null;
  nickname: string | null;
  username: string | null;
  createdAt: string;
  bookCount: number;
  suspended: boolean;
};

export async function getMembers(opts: {
  search?: string;
  page?: number;
  pageSize?: number;
}): Promise<{ rows: MemberRow[]; total: number }> {
  const pageSize = opts.pageSize ?? 50;
  const page = opts.page ?? 1;
  const from = (page - 1) * pageSize;
  const to = from + pageSize - 1;
  const supabase = createAdminClient();

  let query = supabase
    .from('profiles')
    .select(
      'id, email, nickname, username, created_at, status, suspended_until',
      { count: 'exact' }
    )
    .order('created_at', { ascending: false })
    .range(from, to);

  if (opts.search) {
    // PostgREST or 필터 파괴 방지: 쉼표·괄호 제거
    const safe = opts.search.replace(/[,()]/g, '');
    query = query.or(`email.ilike.%${safe}%,nickname.ilike.%${safe}%`);
  }

  const { data: profiles, count } = await query;
  const ids = (profiles ?? []).map((p) => p.id);

  // 등록 책 수 — 이번 페이지 유저만 (관리자 저트래픽이라 클라이언트 집계로 충분)
  const counts = new Map<string, number>();
  if (ids.length > 0) {
    const { data: books } = await supabase
      .from('books')
      .select('user_id')
      .in('user_id', ids);
    for (const b of books ?? []) {
      counts.set(b.user_id, (counts.get(b.user_id) ?? 0) + 1);
    }
  }

  const now = new Date();
  const rows: MemberRow[] = (profiles ?? []).map((p) => ({
    id: p.id,
    email: p.email,
    nickname: p.nickname,
    username: p.username,
    createdAt: p.created_at,
    bookCount: counts.get(p.id) ?? 0,
    suspended: isCurrentlySuspended(p.status, p.suspended_until, now),
  }));

  return { rows, total: count ?? 0 };
}
```

- [ ] **Step 2: getMemberDetail 작성**

`getMemberDetail.ts`:
```ts
import { createAdminClient } from '@/shared/config/supabase/admin';

import { isCurrentlySuspended } from '../lib/suspension';

export type MemberDetail = {
  id: string;
  email: string | null;
  nickname: string | null;
  username: string | null;
  createdAt: string;
  bookCount: number;
  aiUsageCount: number;
  suspended: boolean;
  suspendedUntil: string | null;
};

export async function getMemberDetail(id: string): Promise<MemberDetail | null> {
  const supabase = createAdminClient();

  const { data: p } = await supabase
    .from('profiles')
    .select('id, email, nickname, username, created_at, status, suspended_until')
    .eq('id', id)
    .single();
  if (!p) return null;

  const [books, usage] = await Promise.all([
    supabase.from('books').select('id', { count: 'exact', head: true }).eq('user_id', id),
    supabase.from('ai_usage_logs').select('id', { count: 'exact', head: true }).eq('user_id', id),
  ]);

  return {
    id: p.id,
    email: p.email,
    nickname: p.nickname,
    username: p.username,
    createdAt: p.created_at,
    bookCount: books.count ?? 0,
    aiUsageCount: usage.count ?? 0,
    suspended: isCurrentlySuspended(p.status, p.suspended_until, new Date()),
    suspendedUntil: p.suspended_until,
  };
}
```

- [ ] **Step 3: 타입 확인**

Run: `npm run type-check --workspace=apps/page0127`
Expected: 에러 없음.

- [ ] **Step 4: 커밋**

```bash
git add apps/page0127/src/features/admin-members/api/getMembers.ts apps/page0127/src/features/admin-members/api/getMemberDetail.ts
git commit -m "feat(admin): 회원 목록·상세 쿼리"
```

---

## Task 12: 정지/해제 서버액션

**Files:**
- Create: `apps/page0127/src/features/admin-members/api/suspendActions.ts`

**Interfaces:**
- Consumes: `assertAdmin`(Task 3), `createAdminClient`, `computeBan`·`SuspendInput`(Task 10).
- Produces:
  - `suspendUser(targetUserId: string, input: SuspendInput, reason: string): Promise<void>`
  - `unsuspendUser(targetUserId: string, reason: string): Promise<void>`

- [ ] **Step 1: 서버액션 작성**

`suspendActions.ts`:
```ts
'use server';

import { revalidatePath } from 'next/cache';

import { createAdminClient } from '@/shared/config/supabase/admin';
import { assertAdmin } from '@/shared/lib/admin/assertAdmin';

import { computeBan, type SuspendInput } from '../lib/suspension';

/**
 * 정지: 실제 차단(Auth ban) + 표시 미러(profiles) + 감사 로그를 함께 쓴다.
 * 세 가지가 항상 같이 실행되도록 한 액션에 묶는다(동기화 드리프트 최소화).
 */
export async function suspendUser(
  targetUserId: string,
  input: SuspendInput,
  reason: string
): Promise<void> {
  const admin = await assertAdmin();
  const supabase = createAdminClient();
  const { banDuration, suspendedUntil } = computeBan(input, new Date());

  // 1) 실제 차단
  const { error: banErr } = await supabase.auth.admin.updateUserById(
    targetUserId,
    { ban_duration: banDuration }
  );
  if (banErr) throw new Error(`정지 실패: ${banErr.message}`);

  // 2) 표시용 미러
  await supabase
    .from('profiles')
    .update({ status: 'suspended', suspended_until: suspendedUntil })
    .eq('id', targetUserId);

  // 3) 감사 로그
  await supabase.from('admin_actions').insert({
    admin_email: admin.email,
    target_user_id: targetUserId,
    action: 'suspend',
    reason: reason || null,
    duration_days: input.kind === 'days' ? input.days : null,
  });

  revalidatePath(`/admin/members/${targetUserId}`);
  revalidatePath('/admin/members');
}

export async function unsuspendUser(
  targetUserId: string,
  reason: string
): Promise<void> {
  const admin = await assertAdmin();
  const supabase = createAdminClient();

  const { error: banErr } = await supabase.auth.admin.updateUserById(
    targetUserId,
    { ban_duration: 'none' }
  );
  if (banErr) throw new Error(`해제 실패: ${banErr.message}`);

  await supabase
    .from('profiles')
    .update({ status: 'active', suspended_until: null })
    .eq('id', targetUserId);

  await supabase.from('admin_actions').insert({
    admin_email: admin.email,
    target_user_id: targetUserId,
    action: 'unsuspend',
    reason: reason || null,
    duration_days: null,
  });

  revalidatePath(`/admin/members/${targetUserId}`);
  revalidatePath('/admin/members');
}
```

- [ ] **Step 2: 타입 확인**

Run: `npm run type-check --workspace=apps/page0127`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add apps/page0127/src/features/admin-members/api/suspendActions.ts
git commit -m "feat(admin): 정지/해제 서버액션 — ban+미러+감사로그"
```

---

## Task 13: 회원 목록 페이지 + 테이블

**Files:**
- Create: `apps/page0127/src/features/admin-members/ui/MemberTable.tsx`
- Create: `apps/page0127/app/(admin)/admin/members/page.tsx`

**Interfaces:**
- Consumes: `getMembers`·`MemberRow`(Task 11).

- [ ] **Step 1: MemberTable 작성** (Server Component — 링크만)

`MemberTable.tsx`:
```tsx
import Link from 'next/link';

import type { MemberRow } from '@/features/admin-members/api/getMembers';

export function MemberTable({ rows }: { rows: MemberRow[] }) {
  if (rows.length === 0) {
    return <p className="text-sm text-[var(--ink-soft)]">가입자가 없습니다.</p>;
  }
  return (
    <table className="w-full border-collapse text-sm">
      <thead>
        <tr className="border-b border-[var(--line)] text-left text-[var(--ink-soft)]">
          <th className="py-2">회원</th>
          <th className="py-2">가입일</th>
          <th className="py-2">등록 책</th>
          <th className="py-2">상태</th>
        </tr>
      </thead>
      <tbody>
        {rows.map((m) => (
          <tr key={m.id} className="border-b border-[var(--line)]">
            <td className="py-2">
              <Link href={`/admin/members/${m.id}`} className="hover:underline">
                {m.nickname ?? m.username ?? m.email ?? m.id.slice(0, 8)}
              </Link>
              <div className="text-xs text-[var(--ink-soft)]">{m.email}</div>
            </td>
            <td className="py-2">{m.createdAt.slice(0, 10)}</td>
            <td className="py-2">{m.bookCount}권</td>
            <td className="py-2">
              {m.suspended ? (
                <span className="text-[var(--danger,#a63d10)]">정지</span>
              ) : (
                <span className="text-[var(--ink-soft)]">정상</span>
              )}
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  );
}
```

- [ ] **Step 2: members 페이지 작성** (검색 쿼리파라미터 기반)

`app/(admin)/admin/members/page.tsx`:
```tsx
import { getMembers } from '@/features/admin-members/api/getMembers';
import { MemberTable } from '@/features/admin-members/ui/MemberTable';

export default async function AdminMembersPage({
  searchParams,
}: {
  searchParams: Promise<{ q?: string; page?: string }>;
}) {
  const sp = await searchParams;
  const page = Number(sp.page ?? '1');
  const { rows, total } = await getMembers({ search: sp.q, page });

  return (
    <section>
      <h1 className="mb-4 text-base font-semibold">회원 관리</h1>

      {/* GET 폼 — 검색은 URL 쿼리로. client 컴포넌트 불필요 */}
      <form className="mb-4" action="/admin/members" method="get">
        <input
          name="q"
          defaultValue={sp.q ?? ''}
          placeholder="이메일 또는 닉네임 검색"
          className="w-full max-w-xs rounded border border-[var(--line)] px-3 py-2 text-sm"
        />
      </form>

      <div className="mb-2 text-xs text-[var(--ink-soft)]">총 {total}명</div>
      <MemberTable rows={rows} />
    </section>
  );
}
```

- [ ] **Step 3: 빌드·수동 확인**

Run: `npm run build --workspace=apps/page0127`
개발서버에서 `/admin/members` → 목록·검색·상태 표시 확인.

- [ ] **Step 4: 커밋**

```bash
git add apps/page0127/src/features/admin-members/ui/MemberTable.tsx apps/page0127/app/\(admin\)/admin/members/page.tsx
git commit -m "feat(admin): 회원 목록 페이지·검색"
```

---

## Task 14: 유저 상세 + 정지 폼

**Files:**
- Create: `apps/page0127/src/features/admin-members/ui/SuspendForm.tsx` (`'use client'`)
- Create: `apps/page0127/app/(admin)/admin/members/[id]/page.tsx`

**Interfaces:**
- Consumes: `getMemberDetail`·`MemberDetail`(Task 11), `suspendUser`·`unsuspendUser`(Task 12), `SuspendInput`(Task 10).

- [ ] **Step 1: SuspendForm 작성** (상호작용 필요 → 유일한 `'use client'`)

`SuspendForm.tsx`:
```tsx
'use client';

import { useState, useTransition } from 'react';

import {
  suspendUser,
  unsuspendUser,
} from '@/features/admin-members/api/suspendActions';
import type { SuspendInput } from '@/features/admin-members/lib/suspension';

export function SuspendForm({
  userId,
  suspended,
}: {
  userId: string;
  suspended: boolean;
}) {
  const [reason, setReason] = useState('');
  const [mode, setMode] = useState<'permanent' | 'days'>('permanent');
  const [days, setDays] = useState(7);
  const [isPending, startTransition] = useTransition();

  const onSuspend = () => {
    const input: SuspendInput =
      mode === 'permanent' ? { kind: 'permanent' } : { kind: 'days', days };
    startTransition(() => suspendUser(userId, input, reason));
  };
  const onUnsuspend = () => {
    startTransition(() => unsuspendUser(userId, reason));
  };

  if (suspended) {
    return (
      <div className="rounded border border-[var(--line)] p-4">
        <div className="mb-2 text-sm font-semibold">정지 해제</div>
        <input
          value={reason}
          onChange={(e) => setReason(e.target.value)}
          placeholder="해제 사유(선택)"
          className="mb-2 w-full rounded border border-[var(--line)] px-3 py-2 text-sm"
        />
        <button
          onClick={onUnsuspend}
          disabled={isPending}
          className="rounded border border-[var(--line)] px-3 py-2 text-sm"
        >
          정지 해제
        </button>
      </div>
    );
  }

  return (
    <div className="rounded border border-[var(--line)] p-4">
      <div className="mb-2 text-sm font-semibold">계정 정지</div>
      <div className="mb-2 flex gap-3 text-sm">
        <label className="flex items-center gap-1">
          <input
            type="radio"
            checked={mode === 'permanent'}
            onChange={() => setMode('permanent')}
          />
          영구
        </label>
        <label className="flex items-center gap-1">
          <input
            type="radio"
            checked={mode === 'days'}
            onChange={() => setMode('days')}
          />
          기간
        </label>
        {mode === 'days' && (
          <input
            type="number"
            min={1}
            value={days}
            onChange={(e) => setDays(Number(e.target.value))}
            className="w-20 rounded border border-[var(--line)] px-2 py-1"
          />
        )}
        {mode === 'days' && <span className="self-center">일</span>}
      </div>
      <input
        value={reason}
        onChange={(e) => setReason(e.target.value)}
        placeholder="정지 사유(권장)"
        className="mb-2 w-full rounded border border-[var(--line)] px-3 py-2 text-sm"
      />
      <button
        onClick={onSuspend}
        disabled={isPending}
        className="rounded border border-[var(--line)] px-3 py-2 text-sm"
      >
        정지
      </button>
    </div>
  );
}
```

- [ ] **Step 2: 상세 페이지 작성**

`app/(admin)/admin/members/[id]/page.tsx`:
```tsx
import { notFound } from 'next/navigation';

import { getMemberDetail } from '@/features/admin-members/api/getMemberDetail';
import { SuspendForm } from '@/features/admin-members/ui/SuspendForm';

export default async function AdminMemberDetailPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  const { id } = await params;
  const m = await getMemberDetail(id);
  if (!m) notFound();

  return (
    <section className="flex flex-col gap-6">
      <div>
        <h1 className="text-base font-semibold">
          {m.nickname ?? m.username ?? m.email ?? m.id.slice(0, 8)}
        </h1>
        <p className="text-sm text-[var(--ink-soft)]">{m.email}</p>
      </div>

      <dl className="grid grid-cols-2 gap-3 text-sm sm:grid-cols-4">
        <div>
          <dt className="text-[var(--ink-soft)]">가입일</dt>
          <dd>{m.createdAt.slice(0, 10)}</dd>
        </div>
        <div>
          <dt className="text-[var(--ink-soft)]">등록 책</dt>
          <dd>{m.bookCount}권</dd>
        </div>
        <div>
          <dt className="text-[var(--ink-soft)]">AI 호출</dt>
          <dd>{m.aiUsageCount}회</dd>
        </div>
        <div>
          <dt className="text-[var(--ink-soft)]">상태</dt>
          <dd>{m.suspended ? '정지' : '정상'}</dd>
        </div>
      </dl>

      <SuspendForm userId={m.id} suspended={m.suspended} />
    </section>
  );
}
```

- [ ] **Step 3: 빌드·수동 확인**

Run: `npm run build --workspace=apps/page0127`
개발서버에서 회원 상세 → 정지(영구/기간+사유) → 목록에서 "정지" 표시 확인 → 해제 → "정상" 복귀 확인. `admin_actions`에 2행(정지·해제) 기록 확인.

- [ ] **Step 4: 커밋**

```bash
git add apps/page0127/src/features/admin-members/ui/SuspendForm.tsx apps/page0127/app/\(admin\)/admin/members/\[id\]
git commit -m "feat(admin): 유저 상세·정지/해제 폼"
```

---

## Self-Review 결과

- **Spec 커버리지**: 접근통제(Task 2~4), 셸(Task 5), 마이그레이션(Task 6), 비용 대시보드(Task 7~9), 회원 목록·상세·정지(Task 10~14) — 스펙 4~5장 전 항목이 태스크에 매핑됨. 후속 로드맵(배너 등)은 의도적으로 제외.
- **Placeholder 스캔**: "TBD/적절히 처리" 류 없음. 모든 코드 스텝에 실제 코드 포함.
- **타입 일관성**: `SuspendInput`/`BanParams`(Task 10) → `suspendActions`(Task 12)/`SuspendForm`(Task 14)에서 동일 사용. `MemberRow`/`MemberDetail`(Task 11) → UI/페이지에서 동일. `CostSummary`(Task 8) → `CostDashboard`(Task 9) 일치.
- **주의로 남긴 것**: 색 토큰명(`--line` 등)은 프로젝트 실제 토큰에 맞춰 구현 시 조정. `getMembers` 검색은 PostgREST `or` 필터 파괴 방지를 위해 `,()` 제거.
