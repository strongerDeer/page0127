import { PublicLibrarySkeleton } from '@/widgets/public-library/PublicLibrarySkeleton';

/**
 * /[username] (공개 서재) 라우트 로딩 UI
 */
export default function Loading() {
  return <PublicLibrarySkeleton />;
}
