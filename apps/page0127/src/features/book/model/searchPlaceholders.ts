/**
 * 검색창 placeholder — 실제 책 제목이 회전한다.
 *
 * 왜:
 *   교보문고의 검색창은 "검색어를 입력하세요"가 아니라 **실제 편집 카피**가 돈다.
 *     `금리, 왜 오르기 시작했고 언제까지 오를까?`
 *     `『새의 선물』 은희경 신작`
 *   누군가 이 문구를 골라 넣었다는 티가 난다. 그게 사람이 운영하는 서비스의 냄새다.
 *   (00_docs/07 §5)
 *
 * 규칙:
 * - **실제로 존재하는 책만** 쓴다. 지어내지 않는다.
 * - 장르를 섞는다. 소설·에세이만 있으면 개발서를 찾는 사람이 헤맨다.
 * - 낡으면 갈아 끼운다. 이 파일이 갱신된다는 것 자체가 운영의 흔적이다.
 */
export const SEARCH_PLACEHOLDERS = [
  '『소년이 온다』 한강',
  '미움받을 용기',
  '아주 작은 습관의 힘',
  '클린 코드',
  '『불안의 서』 페소아',
  '팩트풀니스',
] as const;

/** placeholder가 바뀌는 간격(ms). 너무 빠르면 읽기 전에 사라진다. */
export const PLACEHOLDER_ROTATION_MS = 4000;
