# 남은 작업 목록

> **page0127 (페이지 읽다)** — 남은 작업 및 우선순위
>
> 작성일: 2025-12-23 · 최종 수정: **2026-07-14** (순위 변동 배포 반영)
>
> 관련 문서: [07_리디자인_진단_및_실행안.md](./07_리디자인_진단_및_실행안.md) — 왜 고치는지에 대한 근거

---

## 📌 최근 완료 (2026-07-13 ~ 14)

교보문고·밀리의서재를 실측 대조해 "AI 목업처럼 보이는" 원인을 진단하고, Phase 0~3과 Phase 5-1을 실행했다.

| Phase | 내용 | 커밋 |
| --- | --- | --- |
| 0 | 자백 제거 — placeholder 404 버그, Next 기본 파비콘, 데모 SVG, 대시보드 영어, 죽은 약관 링크, 미사용 design-tokens | `1a94b4a` |
| 1 | 디자인 토큰 재설정 — 인디고 → 종이·잉크 팔레트, Geist → Pretendard, 그림자 전면 제거, 표지 셰이프·판형 | `1a94b4a` |
| 2 | 전 화면 이모지 제거(30개 파일 → 0), 2인칭 "당신" 폐지, OG 이미지 재제작 | `3014be1` |
| 3 | 비로그인 공개 영역 개방, 랭킹 카드 링크 복구, `/about`·`/privacy`·`/terms` 실제 작성 | `de8364f` |
| **5-1** | **순위 변동(▲12) — 스냅샷 테이블·일일 집계·UI. DB 적용 및 전 구간 검증 완료** | `5cdbdd2` |

**핵심 발견 두 가지**

1. **"당신"의 근본 원인은 UI가 아니라 AI 프롬프트였다.** `prompts/taste-analysis.ts`가 AI에게 *"사용자를 '당신'으로 부르며"* 라고 지시하고 있어, UI에서 지워도 매번 새로 생성됐다.
2. **Vercel Cron은 GET으로 호출한다.** `/api/notifications/cleanup`이 POST만 export해서 **매일 새벽 2시 cron이 405로 실패**하고 있었다. 알림이 한 번도 정리되지 않았다. → GET 추가로 수정.

### 실측 검증 결과

| 지표 | 전 | 후 |
| --- | --- | --- |
| 폴드(900px) 안의 책 | 0권 | 6권 |
| 랜딩 이미지 | 6개 | 18개 |
| box-shadow | 10개 | **0개** |
| UI 이모지 | 30개 파일 | **0개** |
| "당신" | 12곳 | **0곳** |
| 비로그인 진입 가능 콘텐츠 | **0개** | 4개 |
| 화면의 날짜 문자열 | 0개 | `2026.07.13 기준` |
| **순위 변동** | 없음 | **`▲2` / `−` / `▼2` 렌더 확인** |

---

## ✅ 순위 변동 — 현재 상태

**DB**: 마이그레이션 적용 완료 (`20260713_create_ranking_snapshots.sql`)
**첫 스냅샷**: 2026-07-14, `most` 3행 기록됨 (`best`는 10점 준 책이 아직 없어 0행 — 정상)
**검증**: DB 함수 → cron 엔드포인트 → UI 렌더까지 전 구간 확인
 (어제 날짜에 순위를 뒤집은 테스트 데이터로 `▲2`/`−`/`▼2` 확인 후 삭제)

**Vercel 환경변수**: `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET` 등록 완료

### 남은 확인 (사용자)

```
[ ] Redeploy — 환경변수는 배포 시점에 주입된다.
    환경변수를 추가한 뒤 재배포하지 않으면 cron이 키를 못 찾는다.
    Vercel → Deployments → 최신 배포 ⋯ → Redeploy (빌드 캐시 체크 해제)

[ ] ALADIN_API_KEY 에 Preview 체크 추가 (현재 Production 단독)
    → Preview 배포에서 도서 검색·AI 추천 보강이 실패한다

[ ] 재배포 후 cron 수동 실행해서 200 확인
    응답: { "message": "랭킹 스냅샷 완료 — N행 기록", ... }
```

> **내일(2026-07-15)부터** 랜딩에 진짜 `▲12`가 뜬다.
> 오늘은 비교할 어제 스냅샷이 없어 `has_history=false` → 뱃지가 그려지지 않는다(정직하게 침묵).

### Hobby 플랜 cron 제한 (공식 문서 확인)

| | cron 개수 | 최소 간격 | 정밀도 |
| --- | --- | --- | --- |
| Hobby | 100개 | **하루 1회** | **±59분** |

우리 cron 2개 모두 하루 1회라 제한에 걸리지 않는다. 다만 `00:10` 설정이어도 실제로는 `00:00~00:59` 사이에 돈다. 날짜 단위 스냅샷이라 무해하다.

---

## ⚙️ 환경변수 규칙 (중요)

```
# 서버 전용 — NEXT_PUBLIC_ 접두사를 붙이면 안 된다
OPENAI_API_KEY
ALADIN_API_KEY
SUPABASE_SERVICE_ROLE_KEY
CRON_SECRET

# 공개 — 클라이언트에서 쓴다
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
NEXT_PUBLIC_GA_ID
```

> ⚠️ `NEXT_PUBLIC_` 접두사를 붙이는 순간 Next.js가 그 값을 **클라이언트 번들에 인라인**한다.
> service_role 키는 RLS를 통째로 우회하는 마스터 키다. 브라우저로 나가면 누구나 DB 전체를 읽고 쓸 수 있다.
>
> 2026-07-13에 로컬 `.env`의 이름을 교정했다(백업: `.env.backup-20260713`).
> 검증: service_role 키가 클라이언트 번들(`.next/static`)에 **없음**을 확인했다.

**로컬 빌드**: `.env`의 `OPENAI_API_KEY`가 빈 값이다(키는 Vercel에만 두기로 결정).
`openai/client.ts`가 모듈 로드 시점에 throw하므로 로컬 빌드는 이렇게 돈다:

```bash
OPENAI_API_KEY=dummy npx next build
```

---

## 🔥 다음 작업 (우선순위 순)

### 1. Phase 5 — "운영의 흔적" (가장 임팩트 큼)

> 디자인·카피를 다 고쳐도 이게 없으면 여전히 목업이다.
> 교보 `▲448 급상승`, 밀리 `07월 13일 월요일 업데이트`처럼 **시간을 화면에 새기는** 작업.

```
[완료] 1-1. 순위 변동 (▲12 / ▼3 / NEW)   → 커밋 5cdbdd2, DB 적용·검증 완료
    ├─ [완료] DB: book_ranking_snapshots + snapshot_book_rankings()
    │         + get_book_ranking_with_delta()
    ├─ [완료] 일일 집계: GET /api/cron/snapshot-rankings (KST 00:10, vercel.json)
    ├─ [완료] UI: RankDeltaBadge (▲12 / ▼3 / NEW / −)
    ├─ [완료] 폴백: 함수가 없으면 기존 RPC로 — 코드가 DB보다 먼저 배포돼도 안 깨진다
    └─ [완료] 전 구간 검증 (DB → cron → UI 렌더)
    ※ 남은 건 Redeploy 뿐 (위 "남은 확인" 참조)

[완료] 1-1b. Vercel Cron GET/POST 버그 수정
    └─ /api/notifications/cleanup 이 POST만 export해서 매일 새벽 2시 cron이
       405로 실패하고 있었다. 알림이 한 번도 정리되지 않았다. → GET 추가

[완료] 1-2. 상대 시간 (3시간 전)   → 커밋 a828257, d342477
    ├─ ⚠️ 이미 구현돼 있었다. 다만 **같은 일을 하는 구현이 세 곳**에 흩어져 있었다:
    │      shared/lib/date.ts(자체) / date-fns(CommentItem) / ActivityCard(로컬)
    │      표현도 제각각이었다 ("약 3시간 전" vs "3시간 전")
    ├─ [완료] shared/lib/date.ts 로 일원화 (formatRelativeTime)
    │      · 7일까지 상대 시간, 그 이후는 절대 날짜
    │        ("13개월 전"은 정보가 없다. "2025.06.12"가 유용하다)
    │      · Invalid Date·미래 시각 방어
    ├─ [완료] shared/ui/RelativeTime — <time dateTime title> 시맨틱 태그
    │      hover하면 "2026년 7월 14일 화요일"이 뜬다
    └─ [완료] date-fns 의존성 제거 (코드 사용 0건)

[완료] 1-3. 오늘의 기록 (TodayStrip)   → 커밋 01c937d
    ├─ ⚠️ **"오늘 0권이 완독됐어요"를 쓰지 않는다.**
    │      실제 DB는 완독 4건, 마지막이 6개월 전이다. 그대로 넣으면
    │      살아 있는 서비스가 아니라 **죽은 서비스라는 자백**이 된다.
    ├─ [완료] 오늘 기록이 있으면 오늘 숫자, 없으면 누적으로 물러난다
    │      "지금까지 4권이 쌓였어요 · 2026년 7월 14일 화요일"
    ├─ [완료] 요일이 붙은 날짜는 데이터와 무관하게 매일 바뀐다 (밀리 방식)
    └─ [완료] 기록이 0건이면 스트립 자체를 안 그린다 (자랑할 게 없으면 침묵)

[완료] 1-4. 검색 placeholder 회전   → 커밋 01c937d
    ├─ [완료] 실제 책 제목 6개가 4초마다 회전 (features/book/model/searchPlaceholders.ts)
    ├─ [완료] 포커스·입력 중에는 멈춘다 (힌트가 바뀌면 방해가 된다)
    └─ [완료] prefers-reduced-motion: reduce 면 첫 문구 고정
    ※ 문구가 낡으면 갈아 끼울 것. 그 갱신 자체가 운영의 흔적이다.

[ ] 1-5. 서피스별 정보 밀도 분리          ★ 다음 작업
    ├─ 랜딩·랭킹 카드: 표지+제목+완독수+순위변동 (4필드)
    └─ 책 정보 페이지: 전부 편다 (성향 분포, 메모, 등록일…)
    → 교보는 홈 카드 1~5필드 / 리스트 카드 24필드로 나눈다
    예상: 반나절

[ ] 1-6. best 랭킹이 비어 있다
    └─ 10점을 준 책이 하나도 없어 "10점을 준 사람이 가장 많은 책" 섹션이
       렌더되지 않는다(books.length===0 → null). 데이터가 쌓이면 자동으로 뜬다.
       다만 **오래 비어 있을 섹션이라면** 기준을 낮추거나(9점 이상) 섹션을
       접는 편이 낫다 — 빈 섹션은 "만들다 만" 인상을 준다.
```

**예상**: 나머지 반나절

---

## ⚠️ 데이터가 거의 없다 — 리디자인으로 가릴 수 없는 문제

2026-07-14 실측:

| | 실제 |
| --- | --- |
| 등록된 책 | **4권** |
| 완독 기록 | **4건** |
| 사용자 | **3명** |
| 마지막 완독일 | **2026-01-08** (6개월 전) |

**이건 디자인 문제가 아니다.** 랜딩을 아무리 실서비스처럼 만들어도, 들어가 보면 콘텐츠가 없다.
"폴드 안의 책 6권(목표 8권)"이 미달인 것도, `best` 랭킹 섹션이 통째로 안 뜨는 것도 전부 여기서 온다.

**억지로 채우려 하지 말 것.** "오늘 0권 완독" 같은 문구는 오히려 역효과다(1-3 참조).
지금 코드는 데이터가 쌓이면 **자동으로 채워지도록** 짜여 있다. 필요한 건 코드가 아니라 책이다.

---

### 2. Phase 4 — 이미지 자산 마무리

```
[완료] 히어로 배너에 실제 표지 (HeroBanner)
[완료] 표지 판형 유지 (균일 크롭 해제)
[완료] 표지 없는 책 → 제목 조판 생성 표지
[완료] OG 이미지 (잉크 배경 + 책등 선반)
[완료] 파비콘 (app/icon.svg)

[ ] 2-1. 모노그램 아바타
    └─ 기본 아바타를 닉네임 첫 글자 + 해시 기반 배경색으로
       (회색 사람 실루엣 금지)

[ ] 2-2. 공개 서재의 책등 뷰 강화
    ├─ PublicBookShelf가 이미 spine_image를 쓰고 있다 (rating 5점만 표지)
    └─ "표지 그리드 ↔ 책등 책장" 토글로 만들면 서비스의 시그니처 화면이 된다
       → 알라딘 spineflip은 교보도 밀리도 화면에 안 쓰는 우리만의 자산
```

**예상**: 1일

---

### 3. 남은 화면의 디자인 토큰 적용

Phase 1에서 토큰은 갈아엎었지만, **아직 옛 색을 쓰는 화면이 남아 있다.**

```
[ ] 3-1. 대시보드 차트 색상 점검
    └─ chart-1~5는 종이 팔레트로 바뀌었으나 실제 차트 렌더를 눈으로 확인 안 함
       (로그인 필요해서 이번에 못 봄)
[ ] 3-2. 공개 서재(/[username]) 화면
[ ] 3-3. 피드·알림·설정 화면
[ ] 3-4. StatCard의 9개 legacy variant 이름 정리
    └─ blue/sky/cyan/indigo가 전부 primary로 매핑돼 있다 — 이름이 거짓말을 한다
```

**예상**: 반나절 (로그인해서 각 화면을 눈으로 확인하며)

---

### 4. 문의 창구 (보류 — 사용자 결정 대기)

```
[ ] 4-1. 문의 채널 결정
    → 정해지면 src/widgets/landing/model/siteInfo.ts 의 TODO 한 곳만 보면 된다
    ├─ [ ] SITE_INFO에 CONTACT 추가
    ├─ [ ] SiteFooter에 '문의' 링크 되살리기
    ├─ [ ] app/(public)/contact/page.tsx 생성
    └─ [ ] /privacy에 개인정보 문의처 절 추가 (법적으로 필요)
```

> ⚠️ 현재 `/contact` 라우트와 푸터 링크는 **제거된 상태**다.
> "준비 중" 스텁을 남기는 게 미완성이라는 자백이라서 아예 뺐다.

---

### 5. 환경·인프라 부채

```
[완료] 5-1. 환경변수 이름이 코드와 어긋나 있었다 (2026-07-13 교정)
    ├─ NEXT_PUBLIC_ALADIN_API_KEY  → ALADIN_API_KEY
    ├─ NEXT_PUBLIC_OPENAI_API_KEY  → OPENAI_API_KEY (값은 비어 있음 — 위 규칙 참조)
    └─ NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY → SUPABASE_SERVICE_ROLE_KEY
    → 검증: service_role 키가 클라이언트 번들(.next/static)에 없음을 확인했다.
       (코드에서 참조하지 않아 인라인되지 않았다. 이름 교정으로 영구 격리)

[ ] 5-2. GA4 측정 ID 발급·설정 (NEXT_PUBLIC_GA_ID)
    └─ 구조는 완성. 실제 ID만 없어서 측정이 안 된다.

[ ] 5-3. middleware → proxy 마이그레이션
    └─ Next.js 16이 "middleware 파일 규칙은 deprecated" 경고를 매 부팅마다 낸다.
       src/shared/config/supabase/middleware.ts 의 updateSession 은 그대로 쓰고
       루트 middleware.ts → proxy.ts 로 옮기면 된다.
       ⚠️ PUBLIC_EXCEPTIONS(/books/all, /books/info) 로직을 함께 옮길 것.

[ ] 5-4. .env.example 만들기
    └─ 없다. 새로 클론하면 어떤 키가 필요한지 알 수 없다.
       위 "환경변수 규칙"을 그대로 파일로 만들면 된다(값은 빈 채로).
```

---

### 6. 기능 백로그 (기존)

```
[ ] 6-1. 독서 궁합 E2E 검증
    └─ 구현·정적 검증은 완료. 실제 두 계정으로 UI 확인만 남았다.

[ ] 6-2. 뱃지/업적 시스템
    ├─ DB: user_badges 테이블 + RLS
    ├─ 뱃지 10종 정의 — ⚠️ 이모지 대신 lucide 아이콘으로 (Phase 2 원칙)
    ├─ 획득 로직 (책 추가/완독 시 자동 체크)
    └─ UI: 프로필에 표시
    예상: 4~5시간

[ ] 6-3. 다크 모드 (선택)
    └─ ⚠️ Phase 1에서 반쯤 깨진 잔재(.dark oklch 블록)를 삭제했다.
       지금은 다크 모드가 아예 없는 상태 = 깨끗한 출발점.
       하려면 종이·잉크 팔레트의 다크 버전을 새로 설계해야 한다.
```

---

## 🎯 성공 기준 (재측정용)

리디자인 후 랜딩을 **같은 방식으로 재측정**한다. (측정 방법: playwright로 computed style 전수 조사)

| 지표 | 목표 | 현재 |
| --- | --- | --- |
| 폴드 안의 책 | 8권 이상 | 6권 (DB에 책이 4권뿐이라 한계) |
| 랜딩 이미지 | 20개 이상 | 18개 |
| box-shadow | 2개 이하 | ✅ 0개 |
| 이모지 헤딩 | 0곳 | ✅ 0곳 |
| "당신" | 0회 | ✅ 0회 |
| "준비 중" 페이지 | 0개 | ✅ 0개 |
| 비로그인 진입 가능 콘텐츠 | 3개 이상 | ✅ 4개 |
| **순위 변동 표시** | 있음 | ✅ **`▲2`/`−`/`▼2` 동작 확인** (내일부터 실데이터) |
| 화면의 날짜·시간 문자열 | 3개 이상 | 1개 (`2026.07.13 기준`) → **1-2·1-3에서 해결** |

> **폴드 안의 책·랜딩 이미지**가 목표에 못 미치는 건 디자인 문제가 아니라 **데이터가 부족해서**다.
> DB에 책이 4권뿐이다. 책이 쌓이면 자동으로 채워진다. 억지로 맞추려고 카드를 키우지 말 것.

---

## 📎 참고: 오늘 확립된 원칙

Phase 1~3에서 정한 것. 새 화면을 만들 때 지킨다.

1. **그림자를 쓰지 않는다.** 입체는 `1px solid var(--line)`으로 만든다.
   그림자는 실제로 떠 있어야 하는 것(dialog·dropdown·popover·toast)에만.
2. **표지를 크롭하지 않는다.** 높이(또는 너비)만 고정하고 나머지는 원본 비율대로.
   판형의 불균일이 "진짜 책"의 증거다.
3. **letter-spacing을 건드리지 않는다.** Pretendard는 자간이 이미 최적화돼 있다.
4. **이모지를 헤딩·라벨에 쓰지 않는다.** lucide 단색 아이콘 또는 아이콘 없음.
5. **"당신"을 쓰지 않는다.** 주어를 생략하거나 `{닉네임}님`. AI 프롬프트에도 금지 지침이 있다.
6. **본문을 흐리게 칠하지 않는다.** 읽으라고 쓴 글은 `--text-strong` / `--text-body`.
7. **유채색은 직무를 하나씩 가진다.** 딥그린=완독, 테라코타=순위 상승, destructive=삭제.
8. **채워진 액센트 버튼은 화면당 최대 1개.**
9. **집계 화면에는 기준일을 박는다.** 날짜를 박는다는 건 갱신 책임을 진다는 선언이다.
10. **지어내지 않는다.** 업데이트 로그는 git 이력에서, 개인정보처리방침은 코드에서 확인한 것만.

---

**최종 수정**: 2026-07-14 — Phase 5 (1-2·1-3·1-4) 완료. 다음 작업은 정보 밀도 분리(1-5).
