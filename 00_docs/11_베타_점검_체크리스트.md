# 11. 베타 테스터 모집 전 점검 체크리스트

> page0127 을 **밖의 사람에게 처음 열기 전에** 확인할 것만 모았다. 기능 추가 목록이 아니다 —
> 남은 기능은 `00_남은_작업_목록.md`, 운영 절차는 `apps/page0127/docs/operations-runbook.md`.
>
> 작성: **2026-08-25** · 기준 상태: 운영 `/api/health` = `{"database":"ok","schema":"ok"}`

---

## 이 문서를 쓰는 법

- **P0 는 열기 전에 전부 체크**한다. 하나라도 비어 있으면 모집 링크를 뿌리지 않는다.
- **P1 은 첫 사용자의 첫 5분**에 관한 것. 지금 안 보면 베타가 시작된 뒤엔 못 본다
  (데이터가 쌓이면 "빈 화면"을 다시 만들 수 없다).
- **P2 는 베타를 돌리면서 관찰**한다. 지금 막을 이유는 없지만 모르고 있으면 안 되는 것들.
- 체크할 때 **날짜와 근거를 한 줄** 남긴다. "확인함"만 적으면 3주 뒤에 다시 확인하게 된다.

---

## 🔴 P0 — 열기 전 반드시

사람이 들어오는 순간 터지거나, 터지면 되돌릴 수 없는 것.

- [x] **1. Vercel 이미지 최적화 한도 — 소진 상태였고, 프로필 사진은 조치 완료 (2026-08-25)**
      → PR #115 배포 후 재확인: 아바타가 변환을 거치지 않고 원본으로 나가고 **200 으로 뜬다.**

      **한도는 여전히 초과 상태지만(5,142 / 5,000), 지금 그것 때문에 깨지는 화면은 없다.**
      실측으로 갈렸다 — **한도는 원격 이미지에만 걸린다.**
      | 요청 | 결과 |
      | --- | --- |
      | 원격(Supabase 아바타 · 구글 아바타)을 변환 | **402** — 한도 소진이 그대로 적용 |
      | 로컬 정적(`/images/no-book.jpg`)을 변환 | **200** — `w=48`·`1920`·`3840` 전부. 캐시 여부와 무관 |
      즉 표지(알라딘)·아바타는 변환을 아예 안 타고, 로컬 정적 이미지는 한도를 쓰지 않는다.
      **리셋을 기다릴 이유가 없다.** 리셋일이 궁금하면 Usage 화면 좌상단 드롭다운을
      `Last 30 Days`(롤링 30일 뷰)에서 **청구 주기**로 바꿔야 보인다.

      ⚠️ 앞으로 **새 원격 이미지 호스트를 추가하면 다시 402를 만난다.** 그때는 판정 목록
      (`isPreOptimizedImageSrc`)에 넣을지부터 판단할 것.
      ⚠️ 8/9 하루에만 약 3,600건이 소비됐다(전체 5,142 중). **무엇이 그날 그만큼 태웠는지
      모르는 상태**다 — 사람이 들어오면 재발할 수 있다. Usage → `Projects` 탭이나
      Observability 에서 확인할 것.

      <details><summary>발견 당시 실측 (2026-08-25, 조치 전)</summary>
      운영에서 **새 변환이 전부 402** 다. 캐시에 남은 것만 200 이라 겉으로는 멀쩡해 보인다.
      | 대상 | 결과 |
      | --- | --- |
      | Supabase Storage 아바타 (`/profiles/avatars/...`) | **402** — 지금 공개 서재 프로필 사진이 깨져 있다 |
      | 구글 프로필 사진 (`lh3.googleusercontent.com`) | **402** |
      | 캐시에 남은 로컬 이미지 (`/images/no-book.jpg`) | 200 (기존 캐시 HIT) |
      | 알라딘 표지 | 정상 — `unoptimized` 로 이미 뺐다(PR #112) |
      새로 가입하는 사람의 프로필 사진은 100% 깨지는 상태였다 — 캐시가 없으니 전부 새 변환이다.
      카카오 프로필 사진(`**.kakaocdn.net`)도 같은 경로라 9번(카카오 베타 포함)과 정면으로
      부딪혔다.
      </details>
- [ ] **2. OpenAI 키 잔액·월 한도** — 취향 분석·독서 궁합이 대표 기능인데 401/429 면 첫인상이
      "고장난 사이트"가 된다. 운영 키 만료로 AI 기능 두 개가 전면 중단됐던 전례가 있다.

      **코드 감사 결과 (2026-08-25) — 감지는 되지만 알림이 약하다.**
      | 장치 | 상태 |
      | --- | --- |
      | 401 이 Sentry 이슈가 되는가 | ✅ `catch` → `console.error` → `captureConsoleIntegration(levels:['error'])` |
      | 과금 안 된 실패의 횟수 환불 | ✅ `isUnbilledOpenAiFailure` (4xx·408 제외) |
      | 월 예산 상한 | ✅ 30,000원. 어드민 게이지와 **같은 상수**를 `reserve_ai_usage` 가 쓴다 |
      | 알림 도달 | ⚠️ **규칙이 기본 1개뿐**(4번). 키 만료는 건수가 적어 high priority 판정을 못 받을 수 있다 |
      | 만료 사전 경고 | ❌ 없다 — 죽고 나서야 안다 |
      | 사용자에게 보이는 문구 | ⚠️ "오류가 발생했습니다" — 일시적 오류로 읽혀 **신고 없이 이탈**할 수 있다 |

      → 남은 것은 사람이 해야 한다: **운영에서 취향 분석 1회 실행**(성공하면 키 유효 확인),
      OpenAI 대시보드에서 잔액·키 만료일 확인. 4번(Sentry 알림 규칙)이 이 항목의 실질적 대비책이다.
- [ ] **3. 베타 직전 백업 1회 + 이후 주기 결정** — 조직 플랜이 `free` 라 **자동 백업이 없다.**
      지금까진 내 데이터뿐이라 괜찮았지만, 남의 기록이 들어오는 순간 성격이 달라진다.
      명령은 런북 2장(검증된 `supabase db dump` 2줄). ⚠️ 덤프에 회원 이메일이 들어가므로
      **저장소 폴더 안에 두지 말 것**(public repo).
- [ ] **4. Sentry "새 이슈 발생 시 알림" 규칙 추가** — 현재 규칙이 기본 1개(high priority 판정)
      뿐이라 **건수가 적은 치명적 오류**(키 만료 같은)는 안 걸린다. 토큰이 읽기 전용이라 API 로는
      불가 — Sentry 웹에서 직접.
- [ ] **5. 약관·개인정보처리방침이 현재 기능과 일치하는지** — 아래 세 가지가 문서에 반영돼 있는지.
      - 책 기록을 **AI(OpenAI)에 보낸다**는 점
      - **탈퇴해도 댓글은 남는다**는 점 (`book_comments` 만 `SET NULL`, 나머지는 CASCADE)
      - 문의 창구가 **카카오톡 오픈채팅**이라는 점
- [ ] **6. 화면에 "베타" 표기 추가** — 현재 `베타` 문자열은 `/admin/analytics` 한 곳뿐이다.
      랜딩·헤더에 한 줄: 데이터가 유실될 수 있음 / 무보증 / 피드백 환영.
- [ ] **7. 모바일 실기기 1회 완주** — 지인 베타 유입은 대부분 모바일이다. 폰으로
      가입 → 책 검색·등록 → 완독 → 공개 → 공유까지. **에뮬레이터 말고 실제 폰.**

---

## 🟠 P1 — 첫 5분 경험 (지금 아니면 못 본다)

- [ ] **8. 빈 상태 화면** — 0권 유저가 보는 대시보드·피드·랭킹·취향 분석. 데이터가 적은 **지금이
      유일하게 확인 가능한 시기**다. "오늘 0권 완독" 같은 억지 문구가 없는지도 함께 본다.
- [ ] **9. 카카오 로그인 — 베타에 넣는다** (2026-08-25 결정). 사람들이 카카오로 가장 많이 가입할
      것으로 보고, 기본 흐름은 이미 사용자가 테스트했다. **문제는 베타에서 잡는다.**
      다만 아래는 카카오에만 있는 함정이라 첫 가입자를 특히 지켜본다.
      - 카카오는 **이메일을 아예 주지 않는다** → 계정 연동·온보딩 분기가 구글과 다른 길을 탄다
      - 같은 사람이 구글로 먼저 가입한 뒤 카카오로 들어오면 **별개 계정**이 된다
      - ⚠️ `@repo/icons` 는 CSP 에 막혀 앱에서 못 쓴다 (버튼 아이콘 확인)
- [x] **10. username 정책 — 코드 감사 통과 (2026-08-25)**
      이 항목은 **낡은 정보였다.** 이후에 정책이 들어왔다(`20260805000000_username_change_policy.sql`).
      - **변경 가능하다** — 설정 화면의 `UsernameChangeDialog`, `!changedAt` 조건이라 **평생 1회**
      - **카카오(이메일 없음) 대비돼 있다** — `generateUsernameSeed({email, nickname})` 가
        이메일 → 닉네임 순으로 후보를 훑고, 둘 다 규칙에 안 맞으면 `reader` + 무작위 접미사
      - **예약어를 DB·앱 양쪽이 지킨다** — `is_reserved_username()` 과 `RESERVED_USERNAMES`
      → 남은 것은 실물 확인뿐: **한글 이름 카카오 사용자는 전부 `reader_xxxx` 가 된다.**
        그 사람이 변경 다이얼로그를 찾을 수 있는지, 1회 제한을 이해할 수 있는지.
- [ ] **11. 다크 모드 실물 확인** — 코드는 병합됐으나(OS 설정 기반) 각 화면을 다크로 본 기록이
      없다. 대시보드 차트색(`chart-1~5`)도 함께.
- [x] **12. 책 공개/비공개 기본값 — 코드 감사 통과 (2026-08-25)**
      기본값은 **공개**(`is_public NOT NULL DEFAULT true`, 폼도 `?? true`)지만, 등록 폼에
      공개/비공개 스위치와 `'나만 볼 수 있습니다. (비공개)'` 설명이 **저장 시점 화면에 보인다.**
      코드 주석이 이 위험("사적인 생각을 적고 섹션을 한 번도 열지 않은 채 공개 서재에 게시")을
      이미 인지하고 대응해 뒀다.
- [ ] **13. 문의 동선 — 🔥 로그인 후 화면에 창구가 없다 (2026-08-25 확인)**
      `/contact` 로 가는 링크는 `SiteFooter` 에만 있고, 그 푸터는 **랜딩(`(public)/page.tsx`)과
      문서 페이지(`DocPage`)에만** 붙는다. `AppShell`·`Gnb` 어디에도 문의가 없다.
      즉 **로그인해서 쓰다가 막힌 사람은 화면에서 문의할 방법을 못 찾는다** — 랜딩까지 되돌아가야
      한다. 베타는 피드백이 목적인데 그 통로가 로그인 후 화면에 없는 셈이다.
      → 조치 필요. 가장 싼 방법은 설정 화면이나 프로필 메뉴에 문의 항목 한 줄을 추가하는 것.

---

## 🟡 P2 — 베타를 돌리며 관찰

- [ ] **14. 신고(reports) → 어드민 처리 흐름 1회 리허설** — 남의 글이 처음 생기는 시점이다.
- [ ] **15. rate limit 이 fail-open** — `increment_rate_limit` RPC 가 실패하면 요청을 막지 않는다.
      DB 가 흔들리면 전부 통과한다는 뜻. AI 엔드포인트에 별도 과금 가드가 있는지 재확인.
      (현재 티어: strict 5 / search 20 / standard 60 / beacon 120)
- [ ] **16. 어드민 접근 제어** — 베타 유저가 `/admin` 을 찍어봤을 때.
- [ ] **17. 퍼널 지표** — "몇 명 들어와 몇 명이 첫 책을 등록했나"를 보는 화면이 없다(D-2 미구현).
      최소한 GA·자체 RUM 으로 사후 집계는 되는지 확인해 둔다. 방문 기록은 이미 쌓이는 중이라
      화면은 몇 주 뒤에 만들어도 그때까지가 전부 보인다.
- [ ] **18. uptime 감시 간격** — GitHub Actions cron 이라 실제로는 10~20분 간격이다. 베타 규모면
      충분하나 **알고는 있을 것**. 분 단위가 필요해지면 전용 서비스로 옮긴다.
- [ ] **19. Supabase free 플랜 한도** — DB 용량·대역폭·월 활성 사용자. 베타 인원 대비 여유 확인.

---

## 기록

| 날짜 | 항목 | 결과 |
| --- | --- | --- |
| 2026-08-25 | 문서 작성 | 운영 health 정상(`database:ok`, `schema:ok`) |
| 2026-08-25 | P0-1 이미지 한도 | **소진 상태 확인.** 아바타(Supabase·구글) 새 변환 402. 조치 필요 |
| 2026-08-25 | P0-1 조치 | 프로필 사진 3종을 변환에서 뺌 + 업로드 시 512px WebP 리사이즈 (PR #115) |
| 2026-08-25 | P0-1 운영 재확인 | 배포 후 아바타가 원본 URL 로 나가고 **200** |
| 2026-08-25 | P0-1 한도 범위 규명 | 한도는 **원격 이미지에만** 걸린다(원격 402 / 로컬 정적 200). 리셋 대기 불필요 |
| 2026-08-25 | P0-2 코드 감사 | 401 은 Sentry 이슈가 된다. **알림 규칙(4번)이 실질적 대비책** |
| 2026-08-25 | P1 코드 감사 | 10·12번 통과(체크리스트가 낡았었다). **13번은 조치 필요로 격상** |
